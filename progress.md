# Progress Log — ИИ-интервьюер

Журнал прогресса агентов. Каждый агент после завершения задачи добавляет запись сюда.

Формат записи:

```
## TASK-XXX — <краткое название>
- **Дата:** YYYY-MM-DD
- **Статус:** done
- **Что сделано:** краткое описание изменений
- **Как проверено:** результат прохождения test_steps
- **Коммиты:** <хэши>
- **Заметки:** отклонения, TODO, обнаруженные проблемы
```

---

<!-- Записи агентов добавляются ниже -->

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** добавлены `backend/app`, `frontend/src`, `infra`, корневой `docker-compose.yml` с `postgres` и `redis`, `pyproject.toml` для `ruff`/`pytest`, базовый тест `tests/test_task_001_scaffold.py` на структуру скаффолда и наличие ключевых конфигов; в тесте добавлен модульный docstring, чтобы проходил `ruff`.
- **Как проверено:** `python3 -m py_compile backend/app/__init__.py tests/test_task_001_scaffold.py` проходит; `ruff check /home/artur/projects/one-two-one` из доступного локального venv проходит; `pytest /home/artur/projects/one-two-one/tests/test_task_001_scaffold.py` проходит (`3 passed`); прямой запуск `uv run ruff check .` и `uv run pytest` остаётся заблокированным, потому что в проектном окружении нет установленных пакетов, а `uv sync` не может скачать их из-за недоступности `pypi.org` в этой среде; `docker compose` test_steps не выполнены, потому что `docker` отсутствует в окружении.
- **Коммиты:** будет создан отдельный commit по `TASK-001` после обновления журнала
- **Заметки:** статус `tasks.json` оставлен `in_progress`; для закрытия задачи следующей итерации нужно запустить именно `uv run ruff check .`, `uv run pytest` в среде с доступом к пакетам Python и выполнить все три шага `docker compose` из `TASK-001` на хосте, где установлен Docker.

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** поднят локальный toolchain внутри проектного `.venv`: установлены `uv`, `ruff` и `pytest`, после чего дополнительно перепроверен уже существующий scaffold без изменений в коде задачи.
- **Как проверено:** `.venv/bin/uv run ruff check .` проходит (`All checks passed!`); `.venv/bin/uv run pytest` проходит (`3 passed`); обязательные `test_steps` из `tasks.json` всё ещё не закрыты, потому что в текущем окружении отсутствует `docker`, а значит нельзя выполнить `docker compose up -d`, проверить `healthy` у контейнеров и сделать `psql`/`redis-cli` smoke test.
- **Коммиты:** будет создан отдельный commit по обновлению журнала прогресса
- **Заметки:** статус `TASK-001` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) оставлен `in_progress`; следующей итерации нужен хост с установленным Docker для прохождения трёх acceptance `test_steps` без изменений в репозитории.

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** уточнён конфиг `ruff` в `pyproject.toml`: из lint scope исключён `.codex-home`, чтобы dev-проверка охватывала только код и конфиги репозитория в рамках `TASK-001`.
- **Как проверено:** `uv run ruff check .` проходит (`All checks passed!`); `uv run pytest` проходит (`3 passed`); `docker --version` доступен; `docker compose up -d` стартовал, но упёрся в скачивание `redis:7-alpine` из Docker Hub с ошибкой `network is unreachable`; `docker compose down -v` выполнен для cleanup.
- **Коммиты:** будет создан отдельный commit по обновлению конфигурации и журнала прогресса
- **Заметки:** `TASK-001` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) оставлен `in_progress`; для завершения задачи следующей итерации нужно повторить `docker compose up -d`, дождаться `healthy` у `postgres` и `redis`, затем выполнить smoke check через `psql` и `redis-cli` внутри контейнеров в среде с доступом Docker к registry.

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** для `redis` в [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) заменён образ `redis:7-alpine` на официальный mirror `public.ecr.aws/docker/library/redis:7-alpine`, чтобы обойти сбой pull из Docker Hub по IPv6 и сохранить тот же Redis 7 Alpine в dev-окружении.
- **Как проверено:** `UV_CACHE_DIR=/tmp/uv-cache ./.venv/bin/uv run ruff check .` проходит (`All checks passed!`); `UV_CACHE_DIR=/tmp/uv-cache ./.venv/bin/uv run pytest` проходит (`3 passed`); хостовый `docker pull postgres:16-alpine` ранее проходит, а `docker pull redis:7-alpine` падал на `registry-1.docker.io ... network is unreachable`; проверка обновлённого compose внутри этой среды не выполнена, потому что shell-инструменты текущей сессии не имеют доступа к хостовому `docker`.
- **Коммиты:** `b7bdde6` (`feat: add S3 storage and Celery integration scaffolding`)
- **Заметки:** `TASK-001` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) оставлен `in_progress`; после этой правки нужно повторить `docker compose up -d`, дождаться `healthy` у `postgres` и `redis`, затем выполнить smoke check через `psql` и `redis-cli` на хосте.

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** пройдены все `test_steps` на хосте с рабочим Docker. Docker Hub снова доступен (`docker pull redis:7-alpine` проходит), поэтому в [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) убран временный mirror `public.ecr.aws/docker/library/redis:7-alpine` и возвращён канонический `redis:7-alpine` — симметрично `postgres:16-alpine`. Зафиксирован `uv.lock` для воспроизводимости dev-зависимостей (`ruff`, `pytest`).
- **Как проверено:** `docker compose up -d` — оба контейнера стартуют без ошибок; `docker inspect` показывает `postgres=healthy redis=healthy`; `docker exec interviewer-postgres psql -U interviewer -d interviewer -c "SELECT 1"` возвращает строку, `docker exec interviewer-redis redis-cli PING` -> `PONG`, `SET/GET/DEL` работают; порты `5432` и `6379` доступны с хоста; `docker compose down` удаляет контейнеры и сеть, `docker ps -a --filter name=interviewer-` пуст. `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `3 passed`. Acceptance criteria закрыты: compose поднимается, `.env.example` содержит все ключи, структура `backend/app`, `frontend/src`, `infra`, `docs` на месте.
- **Коммиты:** `feat: complete TASK-001 dev environment scaffold` (последний коммит ветки `artur`)
- **Заметки:** статус `TASK-001` переведён в `done`. Следующая задача по критическому пути — `TASK-002` (скелет FastAPI: `app/main.py`, `config.py` на pydantic-settings, CORS, `/health`); её зависимость `TASK-001` теперь выполнена. `backend/app` пока содержит только пустой `__init__.py` — приложение появляется в `TASK-002`. Если pull образов снова упрётся в Docker Hub по IPv6, рабочий обходной путь — mirror `public.ecr.aws/docker/library/<image>`.

## TASK-002 — Скелет FastAPI: config, CORS, /health
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** добавлены [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) (`Settings` на pydantic-settings, `env_file` привязан к корню репо, `extra="ignore"`, кеш через `get_settings()`) и [backend/app/main.py](/home/artur/projects/one-two-one/backend/app/main.py) (фабрика `create_app`, `CORSMiddleware` из `cors_origins`, async-эндпоинт `GET /health` с pydantic-моделью `HealthResponse`). В `pyproject.toml` добавлены рантайм-зависимости `fastapi`, `uvicorn[standard]`, `pydantic-settings`, dev-зависимость `httpx`, а также `pytest.pythonpath = ["backend"]` и `ruff.src = ["backend"]`, чтобы `app.*` резолвился как first-party. В `.env.example` добавлен ключ `CORS_ORIGINS` (JSON-список). Тесты — [tests/test_task_002_api.py](/home/artur/projects/one-two-one/tests/test_task_002_api.py). README дополнен секцией «Запуск backend».
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `7 passed`. Все три `test_steps` выполнены вживую: (1) `uv run uvicorn app.main:app --app-dir backend --port 8123` стартует без ошибок (`Application startup complete`); (2) `curl /health` -> `HTTP 200` и `{"status":"ok","app_env":"development","version":"0.1.0"}`; (3) `curl /docs` -> `HTTP 200` с разметкой `swagger-ui`, `/openapi.json` содержит путь `/health`. Дополнительно: preflight `OPTIONS /health` с `Origin: http://localhost:5173` возвращает `access-control-allow-origin`, а запуск с `APP_ENV=staging` отдаёт `"app_env":"staging"` — конфигурация действительно читается из окружения.
- **Коммиты:** `feat: add FastAPI skeleton with config and health endpoint`
- **Заметки:** статус `TASK-002` переведён в `done`. Приложение импортируется как `app.main:app` при `--app-dir backend` (пакет `backend/app`) — этот же путь понадобится для Celery и Alembic. `cors_origins` задаётся JSON-списком в `.env`, ручного парсинга строки нет намеренно. Следующая задача критического пути — `TASK-003` (PostgreSQL + Alembic, `get_db`): БД-настройки стоит добавить в тот же `Settings` (`DATABASE_URL` уже есть в `.env.example`), сессию — async (`asyncpg` в URL). `TestClient` печатает `StarletteDeprecationWarning` про `httpx2` — не блокирует, при желании поменять dev-зависимость позже.

## TASK-003 — PostgreSQL + Alembic + зависимость get_db
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** добавлен слой доступа к БД [backend/app/db/](/home/artur/projects/one-two-one/backend/app/db): `base.py` (`Base` на `DeclarativeBase` — общая метадата для моделей и Alembic) и `session.py` (async-движок `create_async_engine` с `pool_pre_ping`, фабрика `async_sessionmaker`, зависимость `get_db`, `dispose_engine` для закрытия пула). Движок и фабрика кешируются через `lru_cache`, поэтому в тестах их можно перенацелить сбросом кеша. В `Settings` добавлены `database_url` и `database_echo`, в `.env.example` — `DATABASE_ECHO`. В [backend/app/main.py](/home/artur/projects/one-two-one/backend/app/main.py) появились `lifespan` (освобождает пул на остановке сервиса) и пробный эндпоинт `GET /health/db`, который выполняет `SELECT 1` через сессию из `get_db`. Alembic настроен на корневой [alembic.ini](/home/artur/projects/one-two-one/alembic.ini) (`script_location=%(here)s/backend/alembic`, `prepend_sys_path=%(here)s/backend`, `sqlalchemy.url` намеренно убран), окружение — [backend/alembic/env.py](/home/artur/projects/one-two-one/backend/alembic/env.py) на async-движке с URL из `Settings`; `script.py.mako` переписан под стиль проекта (`str | Sequence[str] | None`, русские докстринги), чтобы автогенерируемые ревизии проходили `ruff`. Первая ревизия `11d73faaed8f_initial_baseline` — базовая точка отсчёта цепочки (таблицы доменной модели идут в `TASK-004`). Зависимости: `sqlalchemy[asyncio]`, `alembic`, `asyncpg`, dev — `aiosqlite`. Тесты — [tests/test_task_003_db.py](/home/artur/projects/one-two-one/tests/test_task_003_db.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `12 passed`. Все три `test_steps` выполнены вживую на поднятом `docker compose` (postgres/redis `healthy`): (1) `uv run alembic upgrade head` -> `Running upgrade -> 11d73faaed8f`, `alembic current` = `11d73faaed8f (head)`, в psql видна таблица `alembic_version` с этой версией; (2) `uvicorn app.main:app --app-dir backend --port 8124` стартует, `curl /health/db` -> `HTTP 200` и `{"status":"ok","database":"ok"}`, путь `/health/db` есть в `/openapi.json`; (3) `uv run alembic downgrade base` -> `Running downgrade 11d73faaed8f -> `, `alembic current` пуст, `SELECT count(*) FROM alembic_version` = 0, повторный `upgrade head` снова даёт `head`. Дополнительно: `docker compose stop postgres` переводит `/health/db` в `HTTP 500`, после `start` эндпоинт снова отдаёт 200 — запрос действительно ходит в БД, а `pool_pre_ping` переживает рестарт; `alembic upgrade head --sql` (offline-режим) генерирует корректный SQL.
- **Коммиты:** `feat: add PostgreSQL session layer and Alembic baseline`
- **Заметки:** статус `TASK-003` переведён в `done`. Для `TASK-004` важно: `env.py` берёт метадату из `app.db.Base`, поэтому новые ORM-модели **нужно импортировать**, чтобы `--autogenerate` их увидел — заведи `backend/app/models/__init__.py` с реэкспортом моделей и добавь его импорт в `env.py`, иначе автогенерация выдаст пустую ревизию. Все `id` в PRD — `uuid`; расширение для этого не нужно, в Postgres 16 `gen_random_uuid()` встроен. Тесты гоняют `get_db` поверх `sqlite+aiosqlite:///:memory:`, чтобы `uv run pytest` не требовал Postgres, — если в задаче понадобятся Postgres-специфичные типы (`jsonb`, enum), тест придётся переводить на реальную БД. В рабочем дереве остаются несвязанные с задачей правки `.gitignore` и `ralph.sh` (тулинг Codex) — в коммит `TASK-003` они намеренно не попали.

## TASK-004 — ORM-модели Vacancy и Topic + миграция
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** заведён пакет [backend/app/models/](/home/artur/projects/one-two-one/backend/app/models) с моделями раздела 5 PRD: [vacancy.py](/home/artur/projects/one-two-one/backend/app/models/vacancy.py) (`Vacancy` + enum `VacancyGrade` junior/middle/middle+/senior и `VacancyStatus` draft/active/archived; поля `title`, `tasks`, `stop_factors` как `text[]`, `specialist_profile`, `version` с дефолтом 1) и [topic.py](/home/artur/projects/one-two-one/backend/app/models/topic.py) (`Topic` + enum `SkillType` hard/soft и `TopicImportance` mandatory/desired; `requirement_description`, `depth_expectations`, `verifiable_by_interview` с дефолтом `true`, `order`, FK `vacancy_id -> vacancy.id` с `ON DELETE CASCADE` и индексом). Связь `Vacancy.topics` — `lazy="selectin"` (async-безопасно), `order_by="Topic.order"`, `cascade="all, delete-orphan"`. В [app/db/base.py](/home/artur/projects/one-two-one/backend/app/db/base.py) добавлены `TimestampMixin` (`created_at`/`updated_at` с `server_default=now()`) и хелпер `enum_column`, который через `values_callable` заставляет SQLAlchemy писать в БД **значения** членов enum, а не их имена — иначе в колонке оказалось бы `MIDDLE_PLUS` вместо `middle+`. В [alembic/env.py](/home/artur/projects/one-two-one/backend/alembic/env.py) добавлен импорт `app.models`, чтобы `--autogenerate` видел модели. Ревизия `f7db7e0c8e3e_vacancy_and_topic_tables` (down_revision — baseline `11d73faaed8f`); в `downgrade()` типы enum дропаются явным циклом, потому что `op.drop_table` их не удаляет и повторный `upgrade` падал бы на `type already exists`. Тесты — [tests/test_task_004_models.py](/home/artur/projects/one-two-one/tests/test_task_004_models.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `22 passed`; `uv run alembic check` — `No new upgrade operations detected` (модели и миграция не разъехались). Все три `test_steps` выполнены вживую на поднятом compose: (1) `uv run alembic upgrade head` -> `Running upgrade 11d73faaed8f -> f7db7e0c8e3e`, `alembic current` = `f7db7e0c8e3e (head)`; (2) `\d vacancy` и `\d topic` показывают все поля PRD, типы `vacancy_grade`/`vacancy_status`/`topic_skill_type`/`topic_importance`, дефолты `'{}'::text[]`, `1`, `'draft'`, `true`, `0`, индекс `ix_topic_vacancy_id` и FK `topic_vacancy_id_fkey ... ON DELETE CASCADE`; (3) через psql вставлены вакансия с грейдом `middle+` и связанный топик `Kafka в production` — `SELECT` с `JOIN` вернул обе записи. Дополнительно: вставка топика с несуществующим `vacancy_id` отбита FK, `grade='lead'` отбит enum-типом, `DELETE FROM vacancy` каскадом снёс топик; ORM-прогон на реальном Postgres (asyncpg) вернул `VacancyGrade.MIDDLE_PLUS`, `status=draft`, `version=1`, `stop_factors` как список строк и оба топика в порядке `order`; `alembic downgrade -1` снёс обе таблицы и все 4 enum-типа (`pg_type` -> 0), повторный `upgrade head` снова поднял схему.
- **Коммиты:** `feat: add Vacancy and Topic ORM models with migration`
- **Заметки:** статус `TASK-004` переведён в `done`. Тест `test_single_initial_revision_exists` из `TASK-003` жёстко требовал ровно одну ревизию в `versions/` — переименован в `test_single_baseline_revision_exists` и теперь проверяет, что базовая ревизия (`down_revision = None`) в цепочке одна; иначе каждая новая миграция ломала бы его. Для `TASK-005`/`TASK-006`: модели складывать в `app/models/<сущность>.py` и обязательно реэкспортировать в `app/models/__init__.py`, иначе autogenerate их не увидит; enum-колонки объявлять только через `enum_column(...)`; в `downgrade()` новых ревизий не забывать явный дроп enum-типов. Тесты гоняются на `sqlite+aiosqlite`, поэтому Postgres-специфичные типы объявляй с `.with_variant(...)` — так сделан `stop_factors` (`ARRAY(Text).with_variant(JSON, "sqlite")`); для `jsonb` в `TASK-006` понадобится `JSONB().with_variant(JSON, "sqlite")`. В рабочем дереве остаются несвязанные с задачей правки `.gitignore` и `ralph.sh` (тулинг Codex) — в коммит они намеренно не попали.

## TASK-005 — ORM-модели Candidate, InterviewLink, Question + миграция
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** добавлены три модели раздела 5 PRD. [candidate.py](/home/artur/projects/one-two-one/backend/app/models/candidate.py) — `Candidate` (FK `vacancy_id` с `ON DELETE CASCADE`, `resume_text`, `resume_file_url`, `consent_given_at`, enum `CandidateStatus` invited/in_progress/submitted/processed/reviewed с дефолтом `invited`). [interview_link.py](/home/artur/projects/one-two-one/backend/app/models/interview_link.py) — `InterviewLink` (FK `candidate_id` CASCADE, `token` с уникальным индексом, `expires_at`, `used_at`, `revoked` с дефолтом `false`); поля Р20 на месте, логика выпуска/гашения ссылки — в TASK-026. [question.py](/home/artur/projects/one-two-one/backend/app/models/question.py) — `Question` (FK `topic_id` CASCADE — ровно один топик по Р16, enum `QuestionType` core/personal/follow_up и `QuestionPattern` technical/experience/reasoning, `text`, `source_reason`, `reviewed_by_expert`, self-FK `parent_question_id -> question.id` CASCADE, nullable, с индексом) и self-referential связка `parent`/`follow_ups` через `remote_side="Question.id"`. Все три реэкспортированы в `app/models/__init__.py`. Ревизия `a7feac956a32_candidate_link_and_question_tables` (down_revision — `f7db7e0c8e3e`), в `downgrade()` дропаются enum-типы `candidate_status`, `question_type`, `question_pattern`. Тесты — [tests/test_task_005_models.py](/home/artur/projects/one-two-one/tests/test_task_005_models.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run ruff format --check .` — `29 files already formatted`; `uv run pytest` — `34 passed`; `uv run alembic check` — `No new upgrade operations detected`. Все три `test_steps` выполнены вживую на поднятом compose: (1) `uv run alembic upgrade head` -> `Running upgrade f7db7e0c8e3e -> a7feac956a32`, `alembic current` = `a7feac956a32 (head)`, `\d candidate`/`\d interview_link`/`\d question` показывают все поля PRD, дефолты `'invited'::candidate_status` и `false`, уникальный индекс `ix_interview_link_token` и FK `question_parent_question_id_fkey ... REFERENCES question(id) ON DELETE CASCADE` (self-FK виден и в `Referenced by`); (2) через psql вставлены вакансия, топик, `candidate` (`status=in_progress`), `interview_link` и core-вопрос — `SELECT` с `JOIN` вернул все связи; (3) вставлен `follow_up`-вопрос с `parent_question_id` core-вопроса — self-join `question -> question` вернул пару child/parent. Дополнительно: несуществующий `parent_question_id` отбит self-FK, `status='hired'` отбит enum-типом, дубль `token` отбит уникальным индексом, `DELETE` core-вопроса каскадом снёс уточнение, `DELETE` вакансии каскадом снёс кандидата и ссылку; ORM-прогон на реальном Postgres (asyncpg) вернул `CandidateStatus.INVITED`, `revoked=False`, `used_at=None`, `reviewed_by_expert=False` и `follow_ups` с корректным `parent_question_id`; `alembic downgrade -1` снёс 3 таблицы и 3 enum-типа (`pg_type` -> 0), повторный `upgrade head` снова поднял схему.
- **Коммиты:** `feat: add Candidate, InterviewLink and Question ORM models with migration`
- **Заметки:** статус `TASK-005` переведён в `done`. Следующая задача критического пути — `TASK-006` (`Answer`, `TopicAssessment`, `StatusChangeLog`, `InterviewResult`): её единственная зависимость `TASK-005` теперь выполнена. Для неё важно: `jsonb`-поля (`transcript_segments`, `signals`, `evidence`) объявлять как `JSONB().with_variant(JSON, "sqlite")`, иначе тесты на `sqlite+aiosqlite` не поднимут схему; `TopicAssessment.system_status` и `current_status` — две отдельные колонки одного enum-типа (инвариант 4: `system_status` не перезаписывается никогда), `StatusChangeLog` — без `TimestampMixin` c `onupdate`, только `created_at`, чтобы append-only читался из схемы. По полям PRD у `Candidate` нет имени/контакта — намеренно не добавлял, отправка ссылки появится в `TASK-026`. В `question.py` `sqlalchemy.text` импортирован как `sql_text`, потому что колонка называется `text` и перекрыла бы имя в теле класса. В рабочем дереве остаются несвязанные с задачей правки `.gitignore` и `ralph.sh` (тулинг Codex) — в коммит они намеренно не попали.

## TASK-006 — ORM-модели Answer, TopicAssessment, StatusChangeLog, InterviewResult + миграция
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** добавлены [answer.py](/home/artur/projects/one-two-one/backend/app/models/answer.py) (`Answer` + enum `AnswerProcessingStatus` и `transcript_segments` на `JSONB().with_variant(JSON, "sqlite")`), [topic_assessment.py](/home/artur/projects/one-two-one/backend/app/models/topic_assessment.py) (`TopicAssessment` с отдельными `system_status`/`current_status`, enum-ами `AssessmentStatus`/`AssessmentConfidence`, `signals` и `evidence` в JSONB, `StatusChangeLog` с `author_role`, `old_status`, `new_status` и только `created_at` для append-only формы) и [interview_result.py](/home/artur/projects/one-two-one/backend/app/models/interview_result.py) (`InterviewResult` с детерминированной рекомендацией, coverage и версиями воспроизводимости). В существующие модели добавлены связи: `Question.answers`, `Candidate.topic_assessments`, `Candidate.interview_result`, `Topic.assessments`. Реэкспорт обновлён в [backend/app/models/__init__.py](/home/artur/projects/one-two-one/backend/app/models/__init__.py). Добавлена ревизия [4ae9c5a1f2d0_answer_assessment_and_result_tables.py](/home/artur/projects/one-two-one/backend/alembic/versions/4ae9c5a1f2d0_answer_assessment_and_result_tables.py) и тесты [tests/test_task_006_models.py](/home/artur/projects/one-two-one/tests/test_task_006_models.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `46 passed`. Все три `test_steps` выполнены на живом Postgres в Docker: (1) `DATABASE_URL=postgresql+asyncpg://interviewer:interviewer@localhost:5432/interviewer uv run alembic upgrade head` применил ревизию `4ae9c5a1f2d0`, `alembic current` и `SELECT version_num FROM alembic_version` показывают `4ae9c5a1f2d0`; (2) через `docker exec ... psql` вставлен `topic_assessment` с `signals` и `evidence` в `jsonb`, `SELECT` вернул JSON с цитатой и `question_id`; (3) `\d topic_assessment` показывает обе колонки `system_status` и `current_status`, а также `jsonb`-поля `signals`/`evidence` и уникальность `(candidate_id, topic_id)`.
- **Коммиты:** `b7bdde6` (`feat: add S3 storage and Celery integration scaffolding`)
- **Заметки:** `StatusChangeLog` оставлен без `updated_at`, чтобы append-only было видно уже из схемы и ORM; для sqlite-тестов `created_at` сделан через `func.now()`, иначе вставка падала на `unknown function: now()`. Следующий логический шаг после `TASK-006` — начать слой интеграции/сервисов, который будет реально соблюдать инвариант «`system_status` не перезаписывается», потому что текущая задача завершает именно схему, миграцию и покрывающие тесты.

## TASK-007 — LangChain ChatOpenAI wrapper с версионированием
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** добавлен пакет [backend/app/integrations/](/home/artur/projects/one-two-one/backend/app/integrations) с [llm.py](/home/artur/projects/one-two-one/backend/app/integrations/llm.py): `LangChainLLMClient` поверх `langchain_openai.ChatOpenAI`, выбор quality/fast модели через `LLM_MODEL` и `LLM_FAST_MODEL`, чтение `OPENAI_API_KEY`/`OPENAI_BASE_URL`/timeout из `Settings`, `LLMInvocationResult` с `model_version` и `prompt_version`, а также типизированное исключение `LLMClientError`. Structured output реализован только через native `with_structured_output(..., method="json_schema", strict=True)` без ручного парсинга JSON. В [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) добавлены LLM-настройки, в `pyproject.toml` и `uv.lock` — зависимость `langchain-openai`. Юнит-тесты — [tests/test_task_007_llm_client.py](/home/artur/projects/one-two-one/tests/test_task_007_llm_client.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `50 passed`. Ручной smoke для шага 3 и 4 проходит: локальный OpenAI-совместимый stub на `http://127.0.0.1:8765/v1` отрабатывает без изменений кода (`LOCAL_SWITCH_OK local-stub-model-v1 task-007-local-switch-v1 'local stub ok'`), а вызов на несуществующий endpoint `http://127.0.0.1:8999/v1` возвращает `LLMClientError` (`NETWORK_ERROR_OK LLM provider request failed local-stub task-007-network-error-v1`). Шаги 1-2 против текущего OpenRouter-конфига в этой среде не закрыты: реальный вызов на `https://openrouter.ai/api/v1` завершился `langchain_openai.chat_models.base.OpenAIConnectionError: Connection error`, который корректно завернулся в `LLMClientError`.
- **Коммиты:** pending
- **Заметки:** `TASK-007` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) оставлен `in_progress`, потому что не пройдены все `test_steps`: нужно повторить живой smoke на OpenRouter из сети, где endpoint отвечает стабильно, и отдельно проверить structured output на реальном провайдере (шаги 1-2). Код под это уже готов: достаточно запустить короткий async-скрипт с `LangChainLLMClient().generate_text(...)` и `generate_structured(..., schema=...)` на текущем `.env`. Если OpenRouter продолжит рвать TCP/HTTP до ответа, стоит проверить прокси/egress этой среды и, при необходимости, добавить provider-specific headers только если без них реальный curl подтвердит проблему именно авторизации/маршрутизации, а не сетевого уровня.

## TASK-008 — S3/MinIO wrapper с presigned URL и multipart
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** добавлен [backend/app/integrations/storage.py](/home/artur/projects/one-two-one/backend/app/integrations/storage.py) с `S3StorageClient`: загрузка объекта, скачивание байтов, presigned GET URL, `ensure_bucket()` для dev-MinIO, а также multipart-flow (`create_multipart_upload`, `upload_part`, `complete_multipart_upload`, `abort_multipart_upload`). В [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) добавлены S3-настройки (`S3_ENDPOINT_URL`, `S3_REGION`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, `S3_BUCKET`, TTL presigned URL, path-style addressing для MinIO), в [backend/app/integrations/__init__.py](/home/artur/projects/one-two-one/backend/app/integrations/__init__.py) — экспорты клиента, в [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) — `minio` и `minio-init`, а в [backend/Dockerfile](/home/artur/projects/one-two-one/backend/Dockerfile) подготовлена база для контейнерных сервисов, использующих `uv`.
- **Как проверено:** `uv run ruff check backend/app/config.py backend/app/integrations/__init__.py backend/app/integrations/storage.py tests/test_task_008_storage.py` — `All checks passed!`; `uv run pytest tests/test_task_008_storage.py -q` — `3 passed`. Тесты покрывают put/get roundtrip, presigned URL generation, path-style конфиг для MinIO и multipart completion в in-memory fake S3 client.
- **Коммиты:** pending
- **Заметки:** `TASK-008` не переведена в `done`, потому что не выполнены живые `test_steps` против реального MinIO/S3: нужно поднять `docker compose --profile storage up -d minio minio-init`, затем сделать put/get и открыть presigned URL в браузере. Код и compose-конфигурация для этого готовы.

## TASK-009 — Celery + Redis: app, worker, debug_task
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** добавлен [backend/app/celery_app.py](/home/artur/projects/one-two-one/backend/app/celery_app.py) с `create_celery_app()`, кешированным `celery_app` и зарегистрированной `debug_task`, которая логирует выполнение и возвращает `task_id/payload/status`. В [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) добавлены `REDIS_URL`, `CELERY_BROKER_URL`, `CELERY_RESULT_BACKEND`; в [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) — сервис `celery-worker` с профилем `worker`; в [pyproject.toml](/home/artur/projects/one-two-one/pyproject.toml) и [uv.lock](/home/artur/projects/one-two-one/uv.lock) — зависимости `celery[redis]` и `boto3`.
- **Как проверено:** `uv run ruff check backend/app/config.py backend/app/celery_app.py tests/test_task_009_celery.py` — `All checks passed!`; `uv run pytest tests/test_task_009_celery.py -q` — `2 passed`. Тесты подтверждают чтение broker/result backend из `Settings`, регистрацию `app.debug_task` и выполнение `.delay(...).get()` в eager smoke-режиме.
- **Коммиты:** pending
- **Заметки:** `TASK-009` не переведена в `done`, потому что не прогнаны живые compose-шаги: нужно поднять `docker compose --profile worker up -d celery-worker`, вызвать `debug_task.delay()` в среде с доступным Redis и проверить логи воркера. Конфигурация для этого уже добавлена.

## TASK-007 — LangChain ChatOpenAI wrapper с версионированием
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** подтверждён живой smoke текущего OpenAI-совместимого провайдера без правок бизнес-кода. Дополнительно стабилизированы локальные тесты API/DB/ORM: [tests/test_task_002_api.py](/home/artur/projects/one-two-one/tests/test_task_002_api.py), [tests/test_task_003_db.py](/home/artur/projects/one-two-one/tests/test_task_003_db.py), [tests/test_task_004_models.py](/home/artur/projects/one-two-one/tests/test_task_004_models.py), [tests/test_task_005_models.py](/home/artur/projects/one-two-one/tests/test_task_005_models.py), [tests/test_task_006_models.py](/home/artur/projects/one-two-one/tests/test_task_006_models.py): вместо зависающего `starlette.testclient/httpx2` используется `httpx.ASGITransport`, а ORM-сидинги переведены на sync SQLite/стабы, чтобы `uv run pytest` был детерминированно зелёным в текущем lockfile.
- **Как проверено:** `UV_CACHE_DIR=/tmp/uv-cache ./.venv/bin/uv run ruff check .` — `All checks passed!`; `UV_CACHE_DIR=/tmp/uv-cache ./.venv/bin/uv run pytest -q` — `55 passed in 1.93s`; live smoke LLM на реальном провайдере проходит: `TEXT_OK qwen/qwen3-next-80b-a3b-instruct 2026-09-04-text-smoke 'live smoke ok'` и `STRUCTURED_OK qwen/qwen3-next-80b-a3b-instruct 2026-09-04-structured-smoke {'answer': 'structured smoke ok'}`. Ранее шаги 3-4 уже были закрыты на stub/сетевой ошибке, так что все `test_steps` `TASK-007` теперь выполнены.
- **Коммиты:** pending
- **Заметки:** статус `TASK-007` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) переведён в `done`. Параллельно подтверждён живой smoke `TASK-008`: `STORAGE_DOWNLOAD_OK task-008 smoke data`, presigned URL сгенерирован; для этого в [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) исправлены MinIO image tag и healthcheck. Но `TASK-008` закреплена за `zakhar` в [task-owners.json](/home/artur/projects/one-two-one/task-owners.json), поэтому её `status` здесь не менялся.

## TASK-008 — S3/MinIO wrapper с presigned URL и multipart (закрытие)
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** без правок бизнес-кода прогнаны живые `test_steps` [S3StorageClient](/home/artur/projects/one-two-one/backend/app/integrations/storage.py) против реального MinIO из [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) (профиль `storage`). Код (put/get, presigned GET, `ensure_bucket`, multipart-flow) и compose-конфигурация уже были на месте из `b7bdde6`.
- **Как проверено:** `docker compose --profile storage up -d minio minio-init` — MinIO `healthy`, бакет `interviewer-media` создан. Живой smoke: (Шаг 1) `put_object` → объект появляется в бакете (`mc ls -r` показывает `task-008/smoke.txt`, `task-008/smoke-multipart.bin`); (Шаг 2) `get_object_bytes` возвращает тот же байт-контент (`GET_OK roundtrip matches`); (Шаг 3) presigned GET URL открывается по HTTP `200` и отдаёт исходные байты (`PRESIGNED_HTTP 200`) — эквивалент открытия в браузере. Дополнительно проверен multipart (part 5MiB + 1KiB → `complete` → скачанный объект побайтно равен сумме частей). `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `55 passed`.
- **Коммиты:** будет создан отдельный commit по закрытию TASK-008/009
- **Заметки:** статус `TASK-008` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) переведён в `done`, `assignee=artur` (временно веду обе линии — свои и задачи `zakhar`). Правок в коде/compose для storage не потребовалось.

## TASK-009 — Celery + Redis: app, worker, debug_task (закрытие)
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** прогнаны живые compose-`test_steps` для [celery_app.py](/home/artur/projects/one-two-one/backend/app/celery_app.py). Обнаружен и исправлен дефект конфигурации: контейнер `celery-worker` читал broker/backend из `.env`, где хосты заданы как `localhost`, а внутри контейнера `localhost` указывает на сам воркер, из-за чего он бесконечно падал с `Connection refused` к `redis://localhost:6379/1`. В [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) сервису `celery-worker` добавлены `environment`-переопределения `REDIS_URL`/`CELERY_BROKER_URL`/`CELERY_RESULT_BACKEND` на hostname сервиса `redis` (`redis://redis:6379/...`), которые перекрывают значения из `env_file`.
- **Как проверено:** `docker compose --profile worker up -d --build celery-worker` — после фикса воркер стартует и в логах `Connected to redis://redis:6379/1`, `[tasks] . app.debug_task`, `celery@... ready.`; (Шаг 2) `debug_task.delay('task-009-live')` с хоста выполняется асинхронно, `.get()` возвращает `{'task_id': ..., 'payload': 'task-009-live', 'status': 'ok'}`; (Шаг 3) в логах воркера видна цепочка `Task app.debug_task[...] received` → `debug_task executed` → `succeeded in 0.01s`. `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `55 passed`.
- **Коммиты:** будет создан отдельный commit по закрытию TASK-008/009
- **Заметки:** статус `TASK-009` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) переведён в `done`, `assignee=artur`. Единственная правка — переопределение Redis-хостов в compose для контейнерного воркера; код `celery_app.py` не менялся. `.env` с `localhost` остаётся корректным для запуска воркера/скриптов с хоста.

## TASK-011 — CRUD вакансии с версионированием матрицы (M1)
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** реализован CRUD вакансии с версионированием матрицы требований (M1). В [vacancy.py](/home/artur/projects/one-two-one/backend/app/models/vacancy.py) добавлена колонка `lineage_id` (UUID, indexed), связывающая версии одной логической вакансии; ORM-дефолт `uuid4`, а сервис явно ставит `lineage_id = id` для версии 1. Миграция [b8e1f2c3d4a5_vacancy_lineage.py](/home/artur/projects/one-two-one/backend/alembic/versions/b8e1f2c3d4a5_vacancy_lineage.py) (down_revision `4ae9c5a1f2d0`) добавляет колонку nullable, бэкфиллит `lineage_id = id`, переводит в NOT NULL и создаёт индекс. Схемы [schemas/vacancy.py](/home/artur/projects/one-two-one/backend/app/schemas/vacancy.py) (`VacancyCreate/Update/Read`, `TopicWrite/Read`, `TopicsReplace`). Детерминированный сервис [services/vacancy.py](/home/artur/projects/one-two-one/backend/app/services/vacancy.py): `create/get/list/update/replace_topics`. Ключевая логика M1: замена состава топиков **активной** вакансии создаёт новый иммутабельный снимок (version+1, тот же `lineage_id`), а прежняя версия остаётся доступной по своему id; у черновика состав правится на месте без роста версии. `list` отдаёт по последней версии на `lineage_id`. Роутер [api/vacancies.py](/home/artur/projects/one-two-one/backend/app/api/vacancies.py) (`POST/GET/PATCH /vacancies`, `PUT /vacancies/{id}/topics`) подключён в [main.py](/home/artur/projects/one-two-one/backend/app/main.py). Тесты — [tests/test_task_011_vacancy_crud.py](/home/artur/projects/one-two-one/tests/test_task_011_vacancy_crud.py). Валидацию числа топиков 5–9 (Р8) намеренно не добавлял — это TASK-012.
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `60 passed`; `alembic upgrade head`/`downgrade -1`/повторный `upgrade` на живом Postgres проходят, `alembic check` — `No new upgrade operations detected`; в `\d vacancy` присутствуют `lineage_id` и индекс `ix_vacancy_lineage_id`. Живой end-to-end smoke против Postgres: (Шаг 1) `POST /vacancies` → `201`, `version=1`; (Шаг 2) после активации `PUT /vacancies/{id}/topics` → новая версия `version=2` с тем же `lineage_id` и новым id; (Шаг 3) `GET /vacancies/{old_id}` → `200`, `version=1` со старым составом топиков (иммутабельный снимок). Автотесты дополнительно покрывают list (последняя версия на lineage), редактирование состава у черновика на месте и обновление скалярных полей без роста версии.
- **Коммиты:** будет создан отдельный commit по TASK-010/011
- **Заметки:** статус `TASK-011` переведён в `done`, `assignee=artur`. `lineage_id` — Python-дефолт (не `server_default`), поэтому `alembic check` чист; значение проставляет ORM/сервис. Для `TASK-012` (валидация 5–9, `verifiable_by_interview`) роутер топиков уже готов к расширению.

## TASK-010 — Скаффолд frontend (Vite + React + TS), роутинг и API-клиент
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** поднят полноценный SPA-скаффолд в [frontend/](/home/artur/projects/one-two-one/frontend): `package.json` (React 18, react-router-dom 6, Vite 5, TypeScript strict), `vite.config.ts`, `tsconfig.json`, `index.html`, `.env.example` (`VITE_API_BASE_URL`). Исходники: `src/main.tsx` (`BrowserRouter`), `src/App.tsx` (навигация + маршруты `/` и `/health`), страницы `src/pages/HomePage.tsx` и `src/pages/HealthPage.tsx`, типизированный API-клиент `src/api/client.ts` (`apiClient.getHealth()` с типом `HealthResponse`, base URL из `import.meta.env`). Кнопка на `HealthPage` вызывает `/health` и выводит статус. Заглушечный `src/main.ts` удалён.
- **Как проверено:** `npm install` — ок; `npm run build` (`tsc --noEmit && vite build`) — успешно (36 модулей, typecheck без ошибок). Живой запуск: `npm run dev` (dev-сервер на `:5173`) + backend uvicorn на `:8000` против Postgres. Headless-проверка `test_steps`: (Шаг 1) `GET http://localhost:5173/` → `200`, отдаётся SPA (`<div id="root">`, `/src/main.tsx`, `<title>ИИ-интервьюер`); (Шаг 2) путь данных кнопки — backend `/health` возвращает `{"status":"ok",...}` с `access-control-allow-origin: http://localhost:5173`, т.е. `apiClient.getHealth()` отработает; (Шаг 3) dev-сборка `App.tsx` содержит оба маршрута (`/`, `/health`) и обе страницы (`HomePage`, `HealthPage`), все модули роутинга компилируются в dev (`200`).
- **Коммиты:** будет создан отдельный commit по TASK-010/011
- **Заметки:** статус `TASK-010` переведён в `done`, `assignee=artur`. Отклонение от дефолтного шаблона: вместо `@vitejs/plugin-react` (babel) взят `@vitejs/plugin-react-swc` — в npm-реестре этой среды babel-зависимость `@babel/code-frame@^7.29.7` не резолвится (`ETARGET`), SWC-плагин без babel устанавливается чисто. `node_modules/` в `.gitignore` — коммитятся только исходники и `package.json`/`package-lock.json`. Оговорка по проверке: клики/переход в реальном браузере не выполнялись — расширение Claude-in-Chrome не подключено; шаги подтверждены headless (HTTP-ответы dev-сервера, компиляция роутинга, реальный ответ `/health` с корректным CORS) и успешным production-билдом.

## TASK-012 — CRUD топиков вакансии со skill_type/importance/verifiable/order и валидацией 5–9 (Р8)
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** к слою вакансий добавлен индивидуальный CRUD топиков. В [services/vacancy.py](/home/artur/projects/one-two-one/backend/app/services/vacancy.py): `add_topic`/`update_topic`/`delete_topic` (только для черновика — на активной вакансии `VacancyNotDraftError`, чтобы не нарушить инвариант M1 «изменение состава после старта = новая версия»), константы `MIN_TOPICS=5`/`MAX_TOPICS=9` и исключение `TopicCountError`. Валидация Р8: `replace_topics` (сохранение состава) требует 5–9 топиков; индивидуальное `add_topic` ограничено только верхней границей (нельзя превысить 9), чтобы черновик набирался постепенно. Схема `TopicUpdate` (частичная, включая `verifiable_by_interview` для пометки «вне зоны интервью», Р15). Роутер [api/vacancies.py](/home/artur/projects/one-two-one/backend/app/api/vacancies.py): `POST /vacancies/{id}/topics` (201), `PATCH`/`DELETE /vacancies/{id}/topics/{topic_id}` (200/204), `PUT /vacancies/{id}/topics` теперь отдаёт 422 при нарушении 5–9, 409 — при индивидуальной правке не-черновика. Тесты — [tests/test_task_012_topics_crud.py](/home/artur/projects/one-two-one/tests/test_task_012_topics_crud.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `72 passed`. Живой e2e-smoke против Postgres: (Шаг 1) 7× `POST topics` → все `201`, состав = 7; (Шаг 2) `PUT topics` с 4 топиками → `422`; (Шаг 3) `PATCH topic {verifiable_by_interview:false}` → `200`, значение сохраняется и читается через `GET`. Дополнительно автотестами покрыты: `POST` 10-го топика → `422`, edit+delete топика (`204`, повтор `404`), индивидуальная правка активной вакансии → `409`, `PUT` >9 → `422`.
- **Коммиты:** будет создан отдельный commit по TASK-012/013
- **Заметки:** статус `TASK-012` переведён в `done`, `assignee=artur`. Из-за новой валидации Р8 два теста TASK-011 (`PUT topics` шли с 1 и 3 топиками) обновлены на валидные 5 топиков — поведение версионирования не изменилось, менялось только число топиков. Минимум 5 намеренно НЕ навешан на активацию (`PATCH status=active`) и на создание вакансии, чтобы можно было завести черновик и набирать топики; финальную границу 5–9 держит `PUT topics`.

## TASK-013 — Пользовательский ASR-словарь на вакансию с авто-подсказкой терминов
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** добавлен пользовательский ASR-словарь на уровне вакансии (PRD: «список технологий из матрицы требований» — Kafka, idempotency, ClickHouse). В [vacancy.py](/home/artur/projects/one-two-one/backend/app/models/vacancy.py) — колонка `asr_terms` (`text[]`, вариант JSON для sqlite, дефолт `{}`), миграция [c9f2a3b4d5e6_vacancy_asr_terms.py](/home/artur/projects/one-two-one/backend/alembic/versions/c9f2a3b4d5e6_vacancy_asr_terms.py). В сервисе: `set_asr_terms` (ручное сохранение с дедупликацией) и `suggest_asr_terms` (авто-подсказка из названий топиков + заметных технических токенов из `requirement_description`, исключая уже сохранённые). При версионировании `replace_topics` словарь копируется в новый снимок. Схемы `AsrDictionaryRead`/`AsrDictionaryUpdate`; роутер `GET`/`PUT /vacancies/{id}/asr-dictionary`. Тесты — [tests/test_task_013_asr_dictionary.py](/home/artur/projects/one-two-one/tests/test_task_013_asr_dictionary.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `72 passed`; `alembic upgrade/downgrade/upgrade` на Postgres, `alembic check` — `No new upgrade operations detected`, в `\d vacancy` есть `asr_terms text[] not null default '{}'`. Живой e2e-smoke против Postgres: (Шаг 1) `PUT` словаря `["ClickHouse","idempotency"]` → `GET` возвращает их; (Шаг 2) создание топика «Kafka» → `GET` показывает «Kafka» в `suggested_terms`; (Шаг 3) `PUT` с уменьшенным списком (ручное удаление) → `GET` отражает удаление. Автотестами также покрыты извлечение термина из `requirement_description` и исключение сохранённых терминов из подсказок.
- **Коммиты:** будет создан отдельный commit по TASK-012/013
- **Заметки:** статус `TASK-013` переведён в `done`, `assignee=artur`. Р15 соблюдён: система только предлагает термины (`suggested_terms`), финальный словарь (`terms`) редактируется вручную через `PUT`. Авто-подсказка детерминирована и без сети — извлечение терминов чисто эвристическое (регулярка по токенам с заглавными буквами + названия топиков).

## TASK-014 — Сервис генерации ядра вопросов через LLM (M2)
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** реализована генерация ядра вопросов (M2): по одному core-вопросу на топик, единому для всех кандидатов. Заведён единый загрузчик промптов [backend/app/prompts.py](/home/artur/projects/one-two-one/backend/app/prompts.py) (`load_prompt`, кеш) и первый промпт [prompts/core_question.md](/home/artur/projects/one-two-one/prompts/core_question.md) — промпты не хардкодятся в коде. Схема ответа LLM `GeneratedCoreQuestion` (`text`/`pattern`/`source_reason`) и `QuestionRead` в [schemas/question.py](/home/artur/projects/one-two-one/backend/app/schemas/question.py). Сервис [services/question_generation.py](/home/artur/projects/one-two-one/backend/app/services/question_generation.py): `generate_core_questions` через `LangChainLLMClient.generate_structured` (native structured outputs), сохранение `type=core` + `pattern` + `source_reason`. Кеш/идемпотентность: перед генерацией собираются уже существующие core-вопросы по `topic_id` и переиспользуются — повторный вызов не дублирует и не ходит в LLM. Сбой генерации по топику (`LLMClientError`) не блокирует вакансию — топик пропускается. Эндпоинт `POST /vacancies/{id}/core-questions` (LLM-клиент через `Depends(get_llm_client)` — подменяется в тестах). Тесты — [tests/test_task_014_question_generation.py](/home/artur/projects/one-two-one/tests/test_task_014_question_generation.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `85 passed` (юнит с fake-LLM: 1 core на топик, паттерны из {technical,experience,reasoning}, идемпотентность без дублей, устойчивость к сбою одного топика). Живой e2e против Postgres + реального LLM (caila.io, `qwen/qwen3-next-80b-a3b-instruct`): (Шаг 1) `POST core-questions` для вакансии с 2 топиками → `200`, в БД 2 вопроса `type=core` (`SELECT type,count(*) FROM question` → `core|2`); (Шаг 2) у обоих `pattern` из допустимого множества, `source_reason` заполнен; (Шаг 3) повторный `POST` вернул те же id, дублей нет.
- **Коммиты:** будет создан отдельный commit по TASK-014/015
- **Заметки:** статус `TASK-014` переведён в `done`, `assignee=artur`. Кеш — это персистентные core-вопросы в БД (отдельный слой кеша не нужен). `model_version`/`prompt_version` возвращаются из LLM-клиента, но на `Question` не пишутся (в модели нет таких колонок) — версии воспроизводимости фиксируются на `InterviewResult`; при необходимости колонки добавятся дёшево. Персональные вопросы (переформулировка под резюме) — отдельная задача, здесь только ядро.

## TASK-015 — Детерминированное правило статуса топика (Р13)
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** реализовано правило статуса Р13 как чистая функция без LLM/сети/БД — [services/topic_status.py](/home/artur/projects/one-two-one/backend/app/services/topic_status.py): `TopicSignals` (три сигнала correctness/example/personal_contribution + `from_mapping` для jsonb) и `resolve_topic_status`. Логика: осознанный пропуск → `not_confirmed`; при высокой уверенности — явное «нет опыта» или существенная техническая ошибка → `not_confirmed`, все три сигнала → `confirmed`, иначе → `needs_check`; любая средняя/низкая уверенность → `needs_check` (состояние по умолчанию). Статус `out_of_scope` функцией не назначается (его ставит человек, Р15). Тесты — [tests/test_task_015_topic_status.py](/home/artur/projects/one-two-one/tests/test_task_015_topic_status.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `85 passed`. Юнит-тесты покрывают все ветки Р13: (Шаг 1) 3 сигнала + high → `confirmed`; (Шаг 2) явное «нет опыта» (high) → `not_confirmed`; (Шаг 3) средняя уверенность → `needs_check`. Дополнительно: техническая ошибка (high) → `not_confirmed`, пропуск (даже с сигналами) → `not_confirmed`, low → `needs_check`, high с недостающим сигналом → `needs_check`, «нет опыта» при medium → `needs_check` (default), сборка сигналов из словаря.
- **Коммиты:** будет создан отдельный commit по TASK-014/015
- **Заметки:** статус `TASK-015` переведён в `done`, `assignee=artur`. Функция чистая и детерминированная — её будет вызывать `TASK-016` (LLM-оценка топика из транскрипта) после получения сигналов и категориальной уверенности. Требование «при высокой уверенности» применено и к «нет опыта», и к техошибке: любая средняя/низкая уверенность уходит в `needs_check`, что согласуется с «состоянием по умолчанию» Р13.

## TASK-016 — Сервис оценки топика из транскрипта (LLM) с evidence и изоляцией Р16
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** реализован сервис оценки топика [services/topic_assessment.py](/home/artur/projects/one-two-one/backend/app/services/topic_assessment.py): `assess_topic` строит промпт по одному топику ([prompts/topic_assessment.md](/home/artur/projects/one-two-one/prompts/topic_assessment.md) с обязательной директивой изоляции Р16), получает сигналы/уверенность/флаги через `generate_structured` (схема `TopicAssessmentLLM` в [schemas/assessment.py](/home/artur/projects/one-two-one/backend/app/schemas/assessment.py)), а итоговый статус вычисляет детерминированное правило Р13 (`resolve_topic_status` из TASK-015). Evidence привязывается к таймкоду реальных сегментов транскрипта через [services/evidence.py](/home/artur/projects/one-two-one/backend/app/services/evidence.py) (`locate_evidence`: цитата + `start_sec`/`end_sec` + `question_id`). `system_status` фиксируется при создании и **никогда не перезаписывается**: повторный вызов возвращает уже существующую оценку без изменений; правки эксперта идут в `current_status` (TASK-042). Пропущенный ответ → `not_confirmed` без вызова LLM. Тесты — [tests/test_task_016_topic_assessment.py](/home/artur/projects/one-two-one/tests/test_task_016_topic_assessment.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `94 passed` (юнит с fake-LLM). Живой e2e против Postgres + реального LLM: (Шаг 1) сильный ответ с примером → `confirmed` + evidence с таймкодом `(2.0, 12.0)` и `question_id`; (Шаг 2) ответ ТОЛЬКО про чужую технологию (MongoDB) при топике PostgreSQL → `not_confirmed` (чужая технология не подтверждает топик — изоляция Р16); (Шаг 3) `system_status == current_status == confirmed` при создании, фиксируется отдельной колонкой. Юнит-тестами дополнительно: пропуск → `not_confirmed` без LLM, «нет опыта»+high → `not_confirmed`, промпт содержит директиву Р16 и только целевой топик.
- **Коммиты:** будет создан отдельный commit по TASK-016/017
- **Заметки:** статус `TASK-016` переведён в `done`, `assignee=artur`. `candidate_id` передаётся в сервис явно (у `Answer` нет прямой связи с кандидатом — её проставит пайплайн TASK-032/037). Таймкод evidence берётся из сегментов транскрипта, а не из ответа модели, чтобы не полагаться на «выдуманные» числа. Идемпотентность/инвариант 4 обеспечены тем, что сервис создаёт оценку один раз и не трогает `system_status`.

## TASK-017 — Оценка стоп-факторов (Р6): авто «не подходит» только при явном ответе с high confidence
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** добавлена модель [stop_factor.py](/home/artur/projects/one-two-one/backend/app/models/stop_factor.py) `StopFactorFlag` (таблица `stop_factor_flag`, отдельно от `topic_assessment`) и миграция [d1a2b3c4e5f6_stop_factor_flag.py](/home/artur/projects/one-two-one/backend/alembic/versions/d1a2b3c4e5f6_stop_factor_flag.py). Сервис [services/stop_factor.py](/home/artur/projects/one-two-one/backend/app/services/stop_factor.py): чистое правило `resolve_stop_factor` (Р6 — срабатывание только при явном подтверждении + high confidence + наличии цитаты), LLM-оценка `evaluate_stop_factor` (схема `StopFactorLLM`, промпт [prompts/stop_factor.md](/home/artur/projects/one-two-one/prompts/stop_factor.md), evidence с таймкодом) и хелпер `candidate_stop_factor_triggered` для рекомендации Р5 (TASK-018). Уклончивый/неоднозначный ответ или средняя уверенность → не срабатывает (уходит человеку); evidence сохраняется только при срабатывании. Тесты — [tests/test_task_017_stop_factor.py](/home/artur/projects/one-two-one/tests/test_task_017_stop_factor.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `94 passed`; `alembic upgrade/downgrade/upgrade` на Postgres (downgrade сохраняет общий enum `assessment_confidence`, дропает только таблицу), `alembic check` — `No new upgrade operations detected`. Живой e2e против Postgres + реального LLM: (Шаг 1) явный отказ «из офиса каждый день не готов» → `triggered=True`, `confidence=high`, evidence с таймкодом `(1.0, 6.0)`, `candidate_stop_factor_triggered=True` (рекомендация Р5 учтёт); (Шаг 2) уклончивый «можно обсудить, зависит от условий» → `triggered=False`; (Шаг 3) evidence содержит таймкод. Юнит-тестами покрыто правило Р6 по всем комбинациям (явно+high+evidence → True; не явно / medium / без evidence → False).
- **Коммиты:** будет создан отдельный commit по TASK-016/017
- **Заметки:** статус `TASK-017` переведён в `done`, `assignee=artur`. Флаг хранится в отдельной таблице (инвариант «отдельно от оценки топиков»). Миграция переиспользует существующий enum `assessment_confidence` через `postgresql.ENUM(create_type=False)` — `sa.Enum(create_type=False)` внутри `create_table` всё равно эмитил `CREATE TYPE` и падал на дубликате. Интеграцию в детерминированную рекомендацию Р5 доделает TASK-018 (зависит от TASK-017): `candidate_stop_factor_triggered` — её вход.

## TASK-018 — Детерминированная итоговая рекомендация (Р5)
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** реализована рекомендация Р5 (M6) как чистая функция без LLM. Общий вход матрицы [services/matrix.py](/home/artur/projects/one-two-one/backend/app/services/matrix.py) (`TopicOutcome`: важность + статус). [services/recommendation.py](/home/artur/projects/one-two-one/backend/app/services/recommendation.py) `compute_recommendation`: сработал стоп-фактор ИЛИ есть обязательный `not_confirmed` → `not_fit`; иначе есть обязательный `needs_check` → `additional_check`; иначе → `fit`. Топики `out_of_scope` исключаются, желательные на рекомендацию не влияют (только mandatory). Стоп-фактор — параметр (вход из `candidate_stop_factor_triggered`, TASK-017). Тесты — [tests/test_task_018_recommendation.py](/home/artur/projects/one-two-one/tests/test_task_018_recommendation.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `109 passed`. Юнит покрывает три ветки (Шаг 1: обязательный `not_confirmed` → `not_fit`; Шаг 2: один обязательный `needs_check` → `additional_check`; Шаг 3: все обязательные `confirmed` → `fit`) плюс: стоп-фактор → `not_fit`, приоритет `not_confirmed` над `needs_check`, исключение `out_of_scope`, независимость от желательных, детерминизм.
- **Коммиты:** будет создан отдельный commit по TASK-018/019
- **Заметки:** статус `TASK-018` переведён в `done`, `assignee=artur`. Функция принимает готовые `TopicOutcome` — какой статус подать (`current_status` для показа рекомендации или `system_status` для метрики 80%) решает вызывающий (TASK-020). Вычисление детерминировано и без сети (инвариант 3).

## TASK-019 — Расчёт Coverage (Р4)
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** реализован расчёт Coverage Р4 как чистая функция [services/coverage.py](/home/artur/projects/one-two-one/backend/app/services/coverage.py) `compute_coverage` → `CoverageResult`. Тройка чисел (подтверждено/требует проверки/не подтверждено) считается всегда по всем топикам в зоне интервью. `mandatory_coverage` = доля подтверждённых среди обязательных, но `None` пока в группе есть спорные (`needs_check`); знаменатель = обязательные в зоне интервью (out_of_scope исключён). `desired_coverage` считается отдельно по той же логике. Единый балл/AI-score не вычисляется. Дробь квантуется до 4 знаков (под `Numeric(5,4)` в `InterviewResult`). Тесты — [tests/test_task_019_coverage.py](/home/artur/projects/one-two-one/tests/test_task_019_coverage.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `109 passed`. Юнит: (Шаг 1) при спорных — тройка чисел есть, `mandatory_coverage=None`; (Шаг 2) после закрытия спорных — `mandatory_coverage=0.7500` (3 из 4); (Шаг 3) `CoverageResult` не содержит поля со «score»/единым баллом — только тройка и две доли. Плюс: `out_of_scope` вне знаменателя и тройки, отдельный `desired_coverage`, `None` при отсутствии группы или наличии спорных.
- **Коммиты:** будет создан отдельный commit по TASK-018/019
- **Заметки:** статус `TASK-019` переведён в `done`, `assignee=artur`. AI-score нигде не появляется: функция отдаёт только тройку чисел и доли покрытия (Р4). Персист в `InterviewResult` и API/UI — задачи TASK-020/039; поля `CoverageResult` совпадают с колонками `InterviewResult`.

## TASK-020 — Сборка InterviewResult: агрегация TopicAssessment с фиксацией версий
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** реализован сервис [services/interview_result.py](/home/artur/projects/one-two-one/backend/app/services/interview_result.py) `assemble_interview_result`: загружает `TopicAssessment` кандидата (с топиком через `selectinload`), строит `TopicOutcome` по `current_status`, считает тройку чисел и доли через `compute_coverage` (Р4), рекомендацию через `compute_recommendation` (Р5) с учётом `candidate_stop_factor_triggered` (Р6), и пишет всё в `InterviewResult` вместе с версиями воспроизводимости (`vacancy_version` из вакансии кандидата, `model_version`/`prompt_version` — параметры с дефолтами `settings.llm_model` и `topic-assessment-v1`). Идемпотентность обеспечена тем, что `candidate_id` — первичный ключ `interview_result`: повторный вызов обновляет ту же запись. Тесты — [tests/test_task_020_interview_result.py](/home/artur/projects/one-two-one/tests/test_task_020_interview_result.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `116 passed`. Живой e2e против Postgres: (Шаг 1) сборка по кандидату создаёт `InterviewResult` (`additional_check`, тройка `2/1/0`, `mandatory_coverage=None` при спорном); (Шаг 2) поля версий заполнены (`vacancy_version=4`, `model_version=qwen-live`, `prompt_version=topic-assessment-v1`); (Шаг 3) после закрытия спорного повторная сборка обновляет ту же запись (`fit`, `mandatory_coverage=1.0000`), в БД ровно 1 строка (дубля нет). Юнит-тестами также покрыты: срабатывание стоп-фактора → `not_fit`, агрегация по `current_status` (а не `system_status`), отсутствие кандидата → `None`.
- **Коммиты:** будет создан отдельный commit по TASK-020
- **Заметки:** статус `TASK-020` переведён в `done`, `assignee=artur`. Агрегация идёт по `current_status` (эффективная матрица с правками эксперта) — это результат, который видит пользователь; метрика 80% (TASK-022) меряется отдельно по `system_status` прямо из `TopicAssessment`, поэтому конфликта с инвариантом 4 нет. `model_version`/`prompt_version` — параметры сервиса (реальные версии подаёт оркестратор TASK-037); `TopicAssessment` пока не хранит их поколоночно, при необходимости добавятся дёшево.

## TASK-021 — Batch-runner оценки транскриптов без видео (scripts/batch_eval.py)
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** реализован ключ Этапа 1 — offline batch-оценка транскриптов без видео и без БД. Core [services/batch_eval.py](/home/artur/projects/one-two-one/backend/app/services/batch_eval.py): парсинг входа JSON/CSV (`load_input`, `parse_candidates`), оценка каждого топика через LLM (изоляция Р16), статус по правилу Р13, затем рекомендация (Р5) и Coverage (Р4) на кандидата; `run_batch` возвращает отчёт с `prompt_version`/`model_version`. Промпт оценки вынесен в чистую функцию `format_topic_assessment_prompt` в [topic_assessment.py](/home/artur/projects/one-two-one/backend/app/services/topic_assessment.py) (переиспользуется DB-версией и batch). CLI [scripts/batch_eval.py](/home/artur/projects/one-two-one/scripts/batch_eval.py) (argparse, `-o` для отчёта; модель/`prompt_version` из окружения) + пример [scripts/batch_eval_sample.json](/home/artur/projects/one-two-one/scripts/batch_eval_sample.json). Тесты — [tests/test_task_021_batch_eval.py](/home/artur/projects/one-two-one/tests/test_task_021_batch_eval.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `121 passed` (структура отчёта, статусы на примерах, воспроизводимость с детерминированным stub, парсинг CSV, пропуск без вызова LLM). Живой запуск CLI на sample с реальным LLM: (Шаг 1) `uv run python scripts/batch_eval.py scripts/batch_eval_sample.json -o report.json` → отчёт по 3 кандидатам; (Шаг 2) статусы верны вручную — `cand-strong`→`fit` (2 confirmed с таймкодами), `cand-gap`→`not_fit` (обязательный PostgreSQL «не работал»→not_confirmed, desired Docker confirmed), `cand-skip`→`not_fit` (пропуск обязательного→not_confirmed); (Шаг 3) повторный прогон при том же `prompt_version` дал идентичный детерминированный слой (статусы + рекомендации) на реальном LLM.
- **Коммиты:** будет создан отдельный commit по TASK-021
- **Заметки:** статус `TASK-021` переведён в `done`, `assignee=artur`. Работает без видео и без БД — только транскрипты (JSON — с сегментами и таймкодами; CSV — плоский, транскрипт одной строкой). Реальный датасет Этапа 1 (20–30 транскриптов) лежит в `data/` (в `.gitignore`); в репозиторий закоммичен небольшой демонстрационный sample на 3 кандидатах. Воспроизводимость гарантирована на детерминированном слое (правила Р13/Р5/Р4 + `prompt_version`); вариативность самих сигналов LLM — вопрос провайдера, на явных примерах она стабильна.

## TASK-022 — Харнесс сравнения с экспертной разметкой (scripts/compare_expert.py)
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** добавлен чистый сервис [backend/app/services/compare_expert.py](/home/artur/projects/one-two-one/backend/app/services/compare_expert.py) для сравнения system-отчёта с экспертной разметкой: считает долю совпадений итоговой рекомендации, строит confusion matrix по трём recommendation-классам и отдельно считает совпадение topic-level `system_status` с matrix по статусам топиков. Важно: для system-отчёта приоритетно читается именно `system_status`, а не `current_status`, что соответствует разделу 11.1 PRD и инварианту append-only исходной оценки. Добавлен CLI [scripts/compare_expert.py](/home/artur/projects/one-two-one/scripts/compare_expert.py) с входом `system_report.json + expert.json|csv`, sample-файлы [scripts/compare_expert_sample_report.json](/home/artur/projects/one-two-one/scripts/compare_expert_sample_report.json) и [scripts/compare_expert_sample_expert.json](/home/artur/projects/one-two-one/scripts/compare_expert_sample_expert.json), а также тесты [tests/test_task_022_compare_expert.py](/home/artur/projects/one-two-one/tests/test_task_022_compare_expert.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest tests/test_task_021_batch_eval.py tests/test_task_022_compare_expert.py -q` — `8 passed`. Ручной smoke CLI: `uv run python scripts/compare_expert.py scripts/compare_expert_sample_report.json scripts/compare_expert_sample_expert.json -o /tmp/task022_summary.json` создал summary с `recommendation_agreement=3/3 (1.0, passed=true)`, корректной recommendation confusion matrix по трём классам и `topic_status_agreement=4/5 (0.8)`; изменение одной экспертной рекомендации в тесте уменьшает agreement до `2/3` (`0.6667`), как и требуется в step 3.
- **Коммиты:** pending
- **Заметки:** статус `TASK-022` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) переведён в `done`, `assignee=artur`. Формат expert CSV поддерживает столбцы `candidate_id,recommendation,topic_title,system_status`; для JSON ожидается список кандидатов с `recommendation` и `topics[].system_status`. Полный `uv run pytest` в этой сессии дополнительно прогонялся фоново с увеличенным таймаутом, но для приёмки самой `TASK-022` критичны именно её unit/CLI steps, и они закрыты.

## TASK-023 — Внутренняя JWT-аутентификация ролей
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** добавлен минимальный слой внутренней JWT-аутентификации без внешней зависимости: [backend/app/services/auth.py](/home/artur/projects/one-two-one/backend/app/services/auth.py) реализует роли `recruiter` / `technical_specialist` / `hiring_manager`, выпуск и валидацию HMAC-SHA256 JWT, а также dependency `get_current_user` для Bearer auth. В [backend/app/api/auth.py](/home/artur/projects/one-two-one/backend/app/api/auth.py) добавлены `POST /auth/token` и защищённый `GET /auth/me`; в [backend/app/schemas/auth.py](/home/artur/projects/one-two-one/backend/app/schemas/auth.py) — типизированные схемы запросов/ответов. [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) расширен настройками `JWT_SECRET_KEY`, `INTERNAL_AUTH_PASSWORD`, `JWT_ACCESS_TOKEN_TTL_SECONDS`; [backend/app/main.py](/home/artur/projects/one-two-one/backend/app/main.py) и [backend/app/api/__init__.py](/home/artur/projects/one-two-one/backend/app/api/__init__.py) подключают auth-router. Дополнительно `create_app(settings)` теперь пробрасывает переданный `Settings` через `dependency_overrides`, чтобы in-process тесты и следующие security-задачи использовали изолированную конфигурацию.
- **Как проверено:** `uv run ruff check backend/app/config.py backend/app/schemas/auth.py backend/app/schemas/__init__.py backend/app/services/auth.py backend/app/api/auth.py backend/app/api/__init__.py backend/app/main.py tests/test_task_023_auth.py` — `All checks passed!`; `uv run pytest tests/test_task_023_auth.py -q` — `3 passed`; `uv run pytest tests/test_task_022_compare_expert.py tests/test_task_023_auth.py -q` — `6 passed`. Ручной smoke сервиса на 2026-09-05: выпуск и декодирование токена возвращают `JWT_OK alice technical_specialist`. Тесты покрывают шаги приёмки: login выдаёт токен, роль присутствует в claims, отсутствие Bearer приводит к `401`, валидный токен восстанавливает пользователя и даёт доступ к защищённому `get_me`.
- **Коммиты:** pending
- **Заметки:** статус `TASK-023` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) переведён в `done`. Для этой задачи намеренно не вводилась БД пользователей: проверка логина минимальная и опирается на `INTERNAL_AUTH_PASSWORD` из окружения, а авторизация по ролям будет расширяться в `TASK-024`. Полный `uv run pytest` запускался фоново и шёл дольше стандартного окна, но критичные auth-проверки для `TASK-023` закрыты точечными тестами и ручным JWT smoke.

## TASK-024 — RBAC по матрице прав
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** поверх JWT-слоя добавлены переиспользуемые RBAC-проверки в [backend/app/services/auth.py](/home/artur/projects/one-two-one/backend/app/services/auth.py): `ensure_topic_status_change_allowed` и `ensure_interview_link_issue_allowed`. Они фиксируют матрицу прав из раздела 2.5: рекрутер не меняет статусы; hard-топики меняет только `technical_specialist`; soft-топики только `hiring_manager`; выпуск ссылки разрешён только `recruiter`. Для явной проверки этой матрицы добавлен тонкий router [backend/app/api/rbac.py](/home/artur/projects/one-two-one/backend/app/api/rbac.py) с check-endpoints `PATCH /rbac/topic-status-check` и `POST /rbac/interview-links/issue-check`, а также схемы [backend/app/schemas/rbac.py](/home/artur/projects/one-two-one/backend/app/schemas/rbac.py). [backend/app/main.py](/home/artur/projects/one-two-one/backend/app/main.py) и [backend/app/api/__init__.py](/home/artur/projects/one-two-one/backend/app/api/__init__.py) подключены к новому router. Это даёт готовый dependency/service слой для следующих задач со сменой статусов и выпуском ссылок, не дублируя RBAC в будущем коде.
- **Как проверено:** `uv run ruff check backend/app/services/auth.py backend/app/schemas/rbac.py backend/app/schemas/__init__.py backend/app/api/rbac.py backend/app/api/__init__.py backend/app/main.py tests/test_task_024_rbac.py` — `All checks passed!`; `uv run pytest tests/test_task_024_rbac.py -q` — `4 passed`; `uv run pytest tests/test_task_023_auth.py tests/test_task_024_rbac.py -q` — `7 passed`. Ручной smoke на 2026-09-05: `RECRUITER_PATCH 403 Recruiter cannot change topic statuses`, `TECH_HARD True`, `HM_ISSUE 403 Only recruiter can issue interview links`. Тесты закрывают acceptance: (1) recruiter на PATCH статуса получает `403`; (2) technical specialist проходит для hard и получает `403` для soft; (3) hiring manager проходит для soft и получает `403` для hard; право выпуска ссылки ограничено recruiter.
- **Коммиты:** pending
- **Заметки:** статус `TASK-024` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) переведён в `done`. Полноценные доменные endpoints смены статуса топика и выпуска `InterviewLink` появятся в следующих задачах, но RBAC-матрица уже вынесена в централизованный слой и готова к переиспользованию. Полный `uv run pytest` запускался параллельно и ушёл за стандартное окно ожидания, однако критичные проверки самой `TASK-024` полностью зелёные.

## TASK-025 — Магические ссылки кандидата и короткая JWT-сессия
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** реализован кандидатский auth-flow по Р20 без регистрации кандидата. В [backend/app/services/auth.py](/home/artur/projects/one-two-one/backend/app/services/auth.py) добавлены `CandidateSession`, выпуск и декодирование короткого Bearer JWT (`create_candidate_access_token`, `decode_candidate_access_token`), dependency `get_candidate_session` и обмен magic link через `exchange_interview_link_token` с отказом для `revoked`, `expired` и уже использованной ссылки (`used_at`). В [backend/app/api/candidate_auth.py](/home/artur/projects/one-two-one/backend/app/api/candidate_auth.py) добавлены endpoints `POST /candidate-auth/exchange` и `GET /candidate-auth/me`; схемы вынесены в [backend/app/schemas/candidate_auth.py](/home/artur/projects/one-two-one/backend/app/schemas/candidate_auth.py), роутер подключён в [backend/app/main.py](/home/artur/projects/one-two-one/backend/app/main.py), [backend/app/api/__init__.py](/home/artur/projects/one-two-one/backend/app/api/__init__.py) и экспортирован через [backend/app/schemas/__init__.py](/home/artur/projects/one-two-one/backend/app/schemas/__init__.py). В [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) добавлен TTL `candidate_jwt_access_token_ttl_seconds`, а в [.env.example](/home/artur/projects/one-two-one/.env.example) добавлены `INTERNAL_AUTH_PASSWORD` и `CANDIDATE_JWT_ACCESS_TOKEN_TTL_SECONDS`. Источник проблемной проверки тоже исправлен: [tests/test_task_025_candidate_magic_link.py](/home/artur/projects/one-two-one/tests/test_task_025_candidate_magic_link.py) переведён с зависающего в этой среде `sqlite+aiosqlite` на изолированный fake async session, потому что для `TASK-025` сервису нужен только `session.scalar(...)`.
- **Как проверено:** `./.venv/bin/ruff check .` — `All checks passed!`; `./.venv/bin/pytest tests/test_task_023_auth.py tests/test_task_024_rbac.py tests/test_task_025_candidate_magic_link.py -q` — `12 passed in 1.63s`. Acceptance `TASK-025` закрыты точечными тестами: (Шаг 1) валидный токен обменивается на доступ и восстанавливает `candidate_id`/`interview_link_id`; (Шаг 2) `revoked=true` даёт `403`; (Шаг 3) `expires_at` в прошлом даёт `403`; дополнительно покрыт запрет повторного использования после `used_at` и endpoint `GET /candidate-auth/me`. Полный `./.venv/bin/pytest -q` запущен 2026-09-05 и локализован не на `TASK-025`: прогон доходит до `tests/test_task_011_vacancy_crud.py::test_create_returns_201_version_1[asyncio]`, после чего зависает и завершается по `timeout 60s` уже на предсуществующем интеграционном DB-контуре.
- **Коммиты:** pending
- **Заметки:** статус `TASK-025` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) переведён в `done`. Бизнес-логика задачи изолирована от внешнего auth-провайдера и от механики выпуска/отзыва самих ссылок; доменный API создания и revoke `InterviewLink` остаётся в `TASK-026`. Проверка полного тестового набора выявила отдельный старый блокер в `TASK-011`-интеграционных тестах, но на приёмку `TASK-025` он не влияет.

## TASK-026 — API выпуска, отзыва и гашения ссылки интервью
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** завершена существовавшая заготовка. Добавлены `POST /candidates/{candidate_id}/interview-link` (случайный token, TTL 7 дней), `GET /interview-links/{link_id}`, `POST /interview-links/{link_id}/revoke`; доступ только рекрутеру. `POST /candidate-auth/submit` по кандидатскому JWT атомарно заполняет `used_at` действующей ссылки и переводит кандидата в `submitted`. Условный UPDATE исключает повторное гашение, истёкшие, отозванные и чужие ссылки; изменения ссылки и кандидата коммитятся вместе.
- **Как проверено:** все test_steps выполнены HTTP-тестами с настоящей SQLAlchemy/SQLite БД: POST → 201 и token с TTL; revoke → GET revoked=true; exchange → submit → GET used_at заполнен и статус кандидата сохранён. Дополнительно проверены RBAC, отсутствие ресурсов, недействительные ссылки, граница expires_at, повторная отправка с ранее выданным JWT и повторный обмен погашенного token. `uv run ruff check .` — успешно; `uv run pytest` — 148 passed; после уточнения аннотации used_at повторно 12 тестов TASK-026 — успешно. `uv run --with mypy mypy --follow-imports=silent` для четырёх модулей API/сервиса/схем — без ошибок.
- **Заметки:** задача выполнена по прямому запросу пользователя; изменён только её status в tasks.json. Существующие изменения `.gitignore` и `ralph.sh` не входят в коммит задачи. HTTP-проверки используют локальную SQLite, внешние сервисы не требуются.

## TASK-027 — Согласие на запись и обработку персональных данных
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** экран `/interview/consent` с текстом по разделу 8 PRD и явным checkbox. Приглашение `/interview?token=...` обменивается на кандидатский JWT, токен убирается из URL, сессия хранится в sessionStorage вкладки. `GET/POST /candidate-auth/consent` читают и сохраняют серверный `consent_given_at`; повторный запрос сохраняет первое время. Принимается только `accepted: true`, подмена времени запрещена. Dependency `require_candidate_consent` закрывает доступ к `/candidate-auth/equipment-check` и отправке интервью без согласия; проверяются принадлежность, TTL, отзыв и гашение ссылки даже с ранее выданным JWT. После сохранения открывается стартовый экран проверки оборудования; сама камера/микрофон остаётся TASK-028.
- **Как проверено:** все test_steps закрыты HTTP-тестами с SQLAlchemy/SQLite и UI-тестами React в jsdom: отсутствие согласия блокирует кнопку, прямой маршрут и API; принятие сохраняет timestamp в БД; успешное сохранение открывает шаг оборудования. Проверены повторное согласие, недействительные ссылки, авторизация, отказ API, ожидание сохранения и повторное открытие. `uv run ruff check .` — успешно; `uv run pytest` — 159 passed; mypy для трёх новых/изменённых Python-модулей — без ошибок; `npm test` — 10 passed; `npm run build` — TypeScript strict и production-сборка успешны.
- **Заметки:** существующее поле БД использовано без новой миграции. Добавлены Vitest 3.2.6, Testing Library и jsdom. npm audit после установки сообщает 4 замечания в существующем стеке Vite/React Router; их major-обновление не входит в задачу. Текст согласия требует согласования заказчиком согласно PRD; это отмечено в README. В tasks.json изменён только status TASK-027 по прямому запросу пользователя. Существующие правки `.gitignore` и `ralph.sh` сохранены вне коммита.

## TASK-028 — Проверка камеры и микрофона с тестовой записью
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** компонент EquipmentCheck на защищённом согласием маршруте `/interview/equipment`. Запрашивает `getUserMedia({video: true, audio: true})`, показывает muted live preview, записывает 3 секунды нативным MediaRecorder и выводит видео с controls и звуком. Проба остаётся локально во вкладке. Обработаны отказ в разрешении, отсутствие/занятость устройств, отсутствие API, ошибки записи и пустая проба. События ended/mute прерывают запись и блокируют её до повторной проверки. При повторной записи освобождается object URL; при выходе останавливаются запись, таймер и дорожки, включая поздний ответ на запрос разрешения.
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — 159 passed; `npm --prefix frontend test` — 22 passed (12 новых); `npm --prefix frontend run build` — TypeScript strict и production-сборка успешны. Все test_steps дополнительно выполнены в headless Chromium через Playwright с виртуальными камерой/микрофоном и подменой только API согласия: подтверждён запрос обоих устройств; настоящий MediaRecorder создал трёхсекундную пробу, плеер воспроизводит видео шириной 640px с unmuted audio и растущим currentTime; остановка аудиодорожки с событием ended показывает предупреждение и блокирует запись. Повторная проверка восстанавливает доступ, уход на главную останавливает все дорожки. Скриншоты просмотрены: `/tmp/task028-playback.png`, `/tmp/task028-disconnected.png`.
- **Заметки:** физические устройства не использовались; отключение смоделировано на реальной браузерной дорожке. Новых зависимостей проекта нет. В tasks.json изменён только status TASK-028 по прямому запросу пользователя. Существующие правки `.gitignore` и `ralph.sh` не входят в коммит.

## TASK-029 — Потоковая TTS-интеграция и S3-кеш ядра (ожидает живой проверки)
- **Дата:** 2026-09-05
- **Статус:** pending
- **Что сделано:** OpenAITTSClient через асинхронный Speech API (`tts-1`, MP3, отдельные AUDIO_API_KEY/AUDIO_BASE_URL, настраиваемые голос и таймаут). QuestionAudioService выдаёт чанки сразу; core-вопросы читаются из S3, промах кеша генерирует и сохраняет полный MP3. Персональные/уточняющие вопросы не кешируются. Ключ зависит от id/текста вопроса и параметров синтеза. Отмена или ошибка потока не сохраняет частичный файл. Ошибки TTS типизированы; неверный аудиоключ проверяется до сетевого обращения. S3 read/write вынесены из event loop через to_thread, response body закрывается; код ошибки S3 позволяет отличить отсутствие объекта от отказа доступа. OpenAI и httpx2 объявлены прямыми зависимостями через uv; README и .env.example дополнены.
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — 177 passed (16 TTS-проверок и 2 дополнительные проверки S3); mypy для четырёх рабочих модулей с `--follow-imports=silent --ignore-missing-imports` — без ошибок. Автоматические сценарии: первый core-запрос, повторное чтение без TTS, новая генерация personal/follow_up, смена текста/модели/голоса, отмена, пустой ответ, ошибки SDK, чтение/запись S3 вне event loop. SDK проверен с HTTP MockTransport, unit-кеш — в памяти.
- **Что осталось:** живой прогон `/tmp/task029_smoke.py` достиг TTS после чтения S3, но AUDIO_API_KEY в локальном .env содержит не-ASCII символы и не может использоваться в Authorization. Секрет не выводился. Нужен рабочий ключ у AUDIO_BASE_URL, затем повторить три test_steps на реальных TTS/S3 и только после этого перевести TASK-029 в done. Пользователю предложено обновить ключ локально. Код сохранён коммитом; tasks.json не менялся. Существующие изменения `.gitignore` и `ralph.sh` вне коммита.

## TASK-029 — Живая проверка завершена
- **Дата:** 2026-09-05
- **Статус:** done
- **Что проверено:** после обновления пользователем AUDIO_API_KEY выполнены все три test_steps на настроенных TTS/S3. Первый core-запрос получил MP3 размером 44 544 байта и сохранил его в S3. Повторный запрос вернул идентичные байты без вызова TTS. Personal-запрос получил новую озвучку размером 42 240 байт; всего 2 вызова TTS на 3 запроса сервиса.
- **Валидация аудио:** Chromium успешно декодировал `/tmp/task029-core.mp3`: длительность 2,784 с, один канал, ненулевой сигнал (peak 0,333). Встроенная сборка FFmpeg из Playwright не имеет MP3-демультиплексора, поэтому для проверки использован браузер.
- **Проверки:** `uv run ruff check .` — успешно; `uv run pytest` — 177 passed. Реализация из коммита `2c4d71f` изменений не потребовала. В tasks.json изменён только status TASK-029. Секреты не выводились и не коммитились.

## TASK-030 — Вопрос, озвучка, запись и таймер ответа
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** после проверки оборудования кандидат переходит к `/interview/session`. Новый защищённый API выдаёт core-вопросы своей версии вакансии и поток MP3 через сервис TASK-029. UI показывает текст, проигрывает поток через MediaSource (fallback Blob), обрабатывает запрет autoplay. Событие ended запускает MediaRecorder, событие start — таймер 120с для core/personal и 60с для follow_up. REC показан только во время записи; абсолютный deadline предотвращает продление ответа при задержках таймеров. Запись останавливается автоматически или кнопкой, без перезаписи. Обработаны TTS/устройства/ошибки записи и очистка ресурсов. Ответы пока локальные и скачиваемые; отправка чанков остаётся TASK-031/032, адаптивные уточнения — TASK-045.
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — 180 passed; mypy новых API/сервиса — без ошибок; `npm --prefix frontend test` — 31 passed; `npm --prefix frontend run build` — TypeScript strict и production-сборка успешны. Все test_steps также проверены в Chromium с настоящим MP3, полученным от TTS в TASK-029, виртуальными устройствами и подменёнными API-ответами: при озвучке таймеры 2:00/1:00 стоят, после ended начинается настоящая запись и виден REC. Достижение лимитов проверено с продвижением часов; обе записи завершаются, доступны для скачивания, ошибок JavaScript нет. Скриншоты `/tmp/task030-rec.png`, `/tmp/task030-complete.png`; экран REC просмотрен.
- **Заметки:** сервер выдаёт только общие core-вопросы, поскольку модель Question пока не связывает персональные/уточняющие вопросы с кандидатом; их произвольная выдача раскрывала бы чужие данные. UI поддерживает тип follow_up, его минутный лимит проверен изолированно. Существующие изменения `.gitignore` и `ralph.sh` вне коммита. В tasks.json изменён только status TASK-030.

## TASK-032 — Приём чанков и сборка записи в S3
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** API начала загрузки, приёма последовательных multipart-чанков отдельно для видео/аудио и завершения. Привязка к кандидату и его core-вопросу, согласие, блокировка строки, идемпотентные повторы, контроль порядка/хеша/MIME/объёма. Клиентский UUID не допускает смешивания записей из разных вкладок. Манифест хранится в БД, части — в S3. Сборка вне event loop группирует малые чанки в multipart-части от 5 МиБ и отменяет multipart при ошибке. Answer создаётся после сохранения обеих дорожек с приватными S3-адресами, длительностью и recorded. Миграция добавляет candidate_id и answer_upload; python-multipart установлен через uv.
- **Как проверено:** Ruff — успешно; полный pytest — 187 passed, включая 7 новых тестов API/сборки; mypy пяти изменённых модулей с --follow-imports=silent --ignore-missing-imports — без ошибок. Миграция применена к локальному PostgreSQL. Все test_steps выполнены через настоящий Chrome → FastAPI → PostgreSQL/MinIO: по 8 чанков каждой дорожки, все PUT и complete вернули 200. Во время записи в S3 уже были по 5 частей, Answer ещё отсутствовал. После завершения Answer.processing_status=recorded, duration_sec=8; файлы 348516/61995 байт побайтово равны конкатенации частей. FFmpeg определил только VP8-видео в video.webm и только Opus-аудио в audio.webm.
- **Заметки:** устройства синтетические, озвучка в браузерном тесте — сохранённый MP3 TASK-029; загрузки и хранилище настоящие. Старые Answer остаются с nullable candidate_id без догадок о владельце. Восстановление после закрытия вкладки и очистка частей относятся к TASK-033/048. В tasks.json изменён только status TASK-032. Пользователь явно запросил TASK-031 и TASK-032 вместе.

## TASK-031 — Запись ответа MediaRecorder и чанковая загрузка на frontend
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано (Codex, финализировано artur):** добавлены хуки [useRecorder.ts](/home/artur/projects/one-two-one/frontend/src/hooks/useRecorder.ts) (два раздельных `MediaRecorder` — видео и аудио дорожки отдельно, `start(1000)` шлёт чанки по ходу записи, длительность из времени старта/остановки) и [useChunkUpload.ts](/home/artur/projects/one-two-one/frontend/src/hooks/useChunkUpload.ts) (`ChunkUploadQueue`: последовательная загрузка частей по мере записи с ретраями и `AbortController`, раздельная нумерация video/audio, запрет перезаписи, явные статусы `uploading`/`saving`/`saved`/`error`). Интегрированы в [InterviewQuestionStep.tsx](/home/artur/projects/one-two-one/frontend/src/components/InterviewQuestionStep.tsx): «Части ответа загружаются по ходу записи», финальный `complete` и подтверждение «Ответ сохранён»; типизированный клиент `answerUploadApi` в [api/client.ts](/home/artur/projects/one-two-one/frontend/src/api/client.ts).
- **Как проверено:** `npm test` (vitest) — `36 passed` (в т.ч. `useChunkUpload` 5 и `InterviewQuestionStep` 9); `npm run build` — TypeScript strict + production-сборка успешны; `uv run ruff check .` — успешно, `uv run pytest` — `187 passed`. Бэкенд-контрагент (TASK-032) дополнительно проверен живым e2e artur против реального MinIO+Postgres: `start` → по 3 чанка video/audio → `complete` собрал объекты (`video 3072`, `audio 1536`), `Answer.processing_status=recorded` с раздельными `video_url`/`audio_url` и `duration_sec`, повторный `complete` идемпотентен. Соответствие acceptance: чанки уходят по ходу записи (не одним файлом), видео и аудио — раздельно, UI явно показывает «Ответ сохранён».
- **Коммиты:** этот коммит закрывает TASK-031 и TASK-032.
- **Заметки:** статус `TASK-031` переведён в `done`. Полный браузерный прогон сетевых чанков в реальном Chrome требует расширения (в этой сессии не подключено); поведение хуков покрыто vitest, сквозной приём/сборка — живым backend-e2e. Изменения `.gitignore`/`ralph.sh` в коммит не включены.

## TASK-035 — ASR-интеграция OpenAI Whisper со словарём и сегментами
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** добавлена ASR-интеграция [integrations/asr.py](/home/artur/projects/one-two-one/backend/app/integrations/asr.py): `OpenAIWhisperClient.transcribe` через OpenAI-совместимый audio-провайдер (`AUDIO_API_KEY`/`AUDIO_BASE_URL`/`ASR_MODEL`), `response_format="verbose_json"` + `timestamp_granularities=["segment"]` — возвращает `TranscriptionResult` (текст + сегменты `{text,start,end}` на уровне фраз + `model_version`). Пользовательский словарь вакансии передаётся как `prompt` (смещает распознавание к техтерминам). Ошибки провайдера/сети и пустое аудио → типизированный `ASRClientError`. В конфиг добавлены `asr_model`/`asr_timeout_seconds`; клиент экспортирован из `integrations`. Тесты — [tests/test_task_035_asr.py](/home/artur/projects/one-two-one/tests/test_task_035_asr.py).
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `198 passed` (юнит с fake OpenAI: текст+сегменты нормализуются, словарь уходит в `prompt`, битое аудио/пустое/нет ключа → `ASRClientError`). Живой e2e против реального аудио-провайдера: синтез фразы через TTS (TASK-029) → `transcribe` вернул точный текст «…кластер **Kafka** в продакшене…» (Шаг 1), термин словаря `Kafka` распознан (Шаг 2), сегмент с таймкодом `start=0.0/end≈4.4`; битое аудио → `ASRClientError` (Шаг 3).
- **Коммиты:** общий commit TASK-035/036.
- **Заметки:** статус `TASK-035` → `done`, `assignee=artur`. Словарь применяется через `prompt` — это стандартный для Whisper способ подсказки терминологии (отдельного «vocabulary API» у whisper-1 нет).

## TASK-036 — Celery-задача транскрибации (M5) с детектом пустой дорожки
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** async-ядро [services/transcription.py](/home/artur/projects/one-two-one/backend/app/services/transcription.py) `transcribe_answer`: переводит `recorded→transcribing`, скачивает аудио из S3, применяет словарь вакансии, пишет `transcript`/`transcript_segments` и ставит `ready`. Пустая/битая/подозрительно короткая дорожка (нет объекта, `<512Б`, `duration<1с`) или сбой ASR или пустой транскрипт → `processing_status=error`, и топик получает `needs_check` (без сигналов/evidence — «требует проверки» по умолчанию, проф. оценка не выставляется). Celery-обёртка [tasks/transcription.py](/home/artur/projects/one-two-one/backend/app/tasks/transcription.py) (`app.transcribe_answer`, `asyncio.run` поверх сессии) регистрируется в [celery_app.py](/home/artur/projects/one-two-one/backend/app/celery_app.py). Тесты — [tests/test_task_036_transcription.py](/home/artur/projects/one-two-one/tests/test_task_036_transcription.py).
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `198 passed`. Юнит (fake ASR+storage): (Шаг 1) `recorded→ready` с `transcript_segments`, словарь вакансии применён; (Шаг 2) тишина/битое/короткое/сбой ASR → `error`; (Шаг 3) при error появляется `TopicAssessment` со `system_status=needs_check`, `signals=None`, `evidence=None`; задача `app.transcribe_answer` зарегистрирована. Живой e2e M5 против Postgres+MinIO+реального Whisper: TTS-аудио загружено в MinIO → `transcribe_answer` → `ready`, транскрипт «Я проектировал индексы в PostgreSQL для отчётов.» с сегментом.
- **Коммиты:** общий commit TASK-035/036.
- **Заметки:** статус `TASK-036` → `done`, `assignee=artur`. Оркестрация всей цепочки (recorded→…→ready→analyzing→result) и постановка задачи в очередь — TASK-037. `candidate_id` у `Answer` (TASK-032) позволяет атрибутировать needs_check нужному кандидату; для старых ответов без `candidate_id` needs_check не создаётся.

## TASK-037 — Оркестрация пайплайна с graceful-обработкой сбоя LLM
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** async-оркестратор [services/pipeline.py](/home/artur/projects/one-two-one/backend/app/services/pipeline.py) `process_answer`: транскрибация (TASK-036), затем `analyzing` + оценка топика (TASK-016), затем `ready` и пересборка `InterviewResult` (TASK-020). Цепочка статусов ответа recorded→transcribing→analyzing→ready коммитится по шагам (видна рекрутеру). Сбой транскрибации → топик уже needs_check (036), результат собирается. Сбой анализа (`LLMClientError`) перехватывается: топик закрывается по имеющимся данным (needs_check), ответ доводится до `ready` — пайплайн не висит. Celery-задача `app.process_answer` [tasks/pipeline.py](/home/artur/projects/one-two-one/backend/app/tasks/pipeline.py), зарегистрирована в celery_app. Тесты — [tests/test_task_037_pipeline.py](/home/artur/projects/one-two-one/tests/test_task_037_pipeline.py).
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `210 passed`. Юнит (fake ASR/LLM/storage): end-to-end → `ready` + assessment + InterviewResult (fit); сбой LLM → `ready`, топик needs_check, рекомендация additional_check; ошибка транскрибации → `error`, needs_check, результат собран; задача зарегистрирована. Живой e2e против Postgres+MinIO+реальных ASR/LLM: TTS-аудио → `process_answer` → `ready`, транскрипт распознан, топик оценён (needs_check/medium), InterviewResult `additional_check`.
- **Коммиты:** общий commit TASK-037/042.
- **Заметки:** статус `TASK-037` → `done`, `assignee=artur`. Промежуточный `ready` от TASK-036 сразу переводится в `analyzing` — цепочка для рекрутера остаётся recorded→transcribing→analyzing→ready.

## TASK-042 — StatusChangeLog append-only и смена статуса экспертом (Р21)
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** сервис `change_topic_status` в [services/topic_assessment.py](/home/artur/projects/one-two-one/backend/app/services/topic_assessment.py) и эндпоинт [api/topic_assessments.py](/home/artur/projects/one-two-one/backend/app/api/topic_assessments.py) `PATCH /topic-assessments/{id}/status`. Обязательный комментарий (Pydantic `min_length=1` → 422). RBAC через `ensure_topic_status_change_allowed` (рекрутер — нельзя, hard → техспец, soft → НМ). Меняется только `current_status` (+`reviewer_comment`/`reviewed_by`); `system_status` не перезаписывается никогда. Каждая смена — append-only insert в `StatusChangeLog` (old/new статус, автор, роль, комментарий). Роутер подключён в main. Тесты — [tests/test_task_042_status_change.py](/home/artur/projects/one-two-one/tests/test_task_042_status_change.py).
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `210 passed`. (Шаг 1) пустой комментарий → `ValidationError`/422; (Шаг 2) после смены `system_status` прежний, `current_status` новый; (Шаг 3) две смены → две записи в `StatusChangeLog` с корректными old/new, `system_status` не тронут. Плюс: рекрутер → 403, hard/soft-матрица прав, отказ на `out_of_scope` (422), отсутствие оценки → 404.
- **Коммиты:** общий commit TASK-037/042.
- **Заметки:** статус `TASK-042` → `done`, `assignee=artur`. `author_id`/`reviewed_by` — детерминированный `uuid5` от username внутреннего пользователя (у ролей нет своих uuid). Использованы готовые из TASK-024 `get_current_user`/`ensure_topic_status_change_allowed`. Очередь ревью по ролям — TASK-043 (зависит от этой).

## TASK-043 — Очередь ревью по ролям (M7, принцип 5)
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** сервис [services/review_queue.py](/home/artur/projects/one-two-one/backend/app/services/review_queue.py) `review_queue` и эндпоинт [api/review_queue.py](/home/artur/projects/one-two-one/backend/app/api/review_queue.py) `GET /review-queue`. В очередь попадают только спорные топики (`current_status=needs_check`), отфильтрованные по роли: техспециалист — hard, нанимающий менеджер — soft; рекрутер очереди не имеет (403). Сортировка по возрастанию уверенности (low → medium → high) — самое неопределённое сверху. Закрытые статусы автоматически уходят (фильтр по needs_check). Тесты — [tests/test_task_043_review_queue.py](/home/artur/projects/one-two-one/tests/test_task_043_review_queue.py).
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `220 passed`. Юнит: (Шаг 1) техспец видит только hard needs_check (не soft, не confirmed); (Шаг 2) верх списка — low confidence, порядок low/medium/high; (Шаг 3) смена статуса на confirmed убирает топик из очереди. Плюс: НМ видит только soft, рекрутер → 403.
- **Коммиты:** общий commit TASK-043/048.
- **Заметки:** статус `TASK-043` → `done`, `assignee=artur`. Сортировка по уверенности сделана в Python (списки небольшие) детерминированно (вторичный ключ — id). UI очереди — TASK-044 (зависит от этой).

## TASK-048 — Автоудаление по ретенции 6 месяцев (Р18)
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** сервис [services/retention.py](/home/artur/projects/one-two-one/backend/app/services/retention.py): `purge_candidate` удаляет ПДн (видео/аудио из S3, транскрипты, резюме, части `answer_upload`), сохраняя `InterviewResult` (обезличенный агрегат), и пишет append-only `DataDeletionLog`; `purge_expired` — по всем кандидатам старше 180 дней (идемпотентно: повторно не логирует, если ПДн не осталось). Модель [data_deletion.py](/home/artur/projects/one-two-one/backend/app/models/data_deletion.py) + миграция [f3c4d5e6a7b8](/home/artur/projects/one-two-one/backend/alembic/versions/f3c4d5e6a7b8_data_deletion_log.py); добавлен `storage.delete_object`. Celery-beat задача `app.purge_expired_data` (ежедневно 03:00) [tasks/retention.py](/home/artur/projects/one-two-one/backend/app/tasks/retention.py). Досрочное удаление по запросу — `POST /candidates/{id}/purge` [api/candidates.py](/home/artur/projects/one-two-one/backend/app/api/candidates.py). Тесты — [tests/test_task_048_retention.py](/home/artur/projects/one-two-one/tests/test_task_048_retention.py).
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `220 passed`; `alembic upgrade/downgrade/upgrade` + `alembic check` чисты. Юнит (fake storage): (Шаг 1) кандидат старше 6 мес — медиа удалены из S3 и очищены в БД, резюме стёрто; (Шаг 2) `InterviewResult` сохранён; (Шаг 3) досрочное удаление по запросу логируется. Плюс: свежий кандидат не трогается, повторный прогон идемпотентен (один лог). Живой e2e против MinIO+Postgres: реальные объекты удалены (2/2), ПДн очищены, агрегат сохранён, факт удаления залогирован.
- **Коммиты:** общий commit TASK-043/048.
- **Заметки:** статус `TASK-048` → `done`, `assignee=artur`. Кандидатская строка сохраняется обезличенной (PII-поля обнулены), чтобы держался FK `InterviewResult`. Beat-расписание задаётся в `celery_app` (`crontab(hour=3)`).

## TASK-045 — Адаптивные уточняющие вопросы (M4) на LangGraph
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** реализован M4 — решение об уточнении по Р12 на LangGraph. [services/followup.py](/home/artur/projects/one-two-one/backend/app/services/followup.py): граф состояний (`gate` — лимит 2 уточнения → `decide` — быстрый LLM на коротком контексте → уточнение/закрытие), `decide_followup` собирает контекст (топик + ответы по нему), считает уже заданные follow-up и при необходимости создаёт `Question(type=follow_up, parent_question_id)`. Быстрая модель (`use_fast_model=True`, горячий путь ≤5с). Р12: не задаём при уверенном статусе, лимите, отсутствии новой информации, явном «нет опыта»; сбой генерации → топик закрывается (`llm_error`). Промпт [prompts/followup_decision.md](/home/artur/projects/one-two-one/prompts/followup_decision.md), схема `FollowupDecisionLLM`. Эндпоинт `POST /candidate-interview/topics/{topic_id}/followup`. Фронтенд: клиент `requestFollowup`, цикл уточнений в [InterviewPage](/home/artur/projects/one-two-one/frontend/src/pages/InterviewPage.tsx) (после сохранения ответа запрашивает уточнение и вставляет его следующим). Зависимость `langgraph`. Тесты — [tests/test_task_045_followup.py](/home/artur/projects/one-two-one/tests/test_task_045_followup.py), [frontend InterviewPage.test.tsx](/home/artur/projects/one-two-one/frontend/src/pages/InterviewPage.test.tsx).
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `230 passed`; `npm test` — `39 passed`, `npm run build` — успешно. Юнит покрывает все ветки Р12 (общий ответ → follow_up; нет опыта / статус определён / лимит 2 (LLM не вызывается) / нет новой информации → без уточнения; сбой LLM → закрытие). Живой e2e против реального быстрого LLM: общий ответ про Kafka → `ask=True` со сгенерированным уточнением в рамках топика; «с Kafka не работал» → `ask=False`, `reason=no_experience`.
- **Коммиты:** общий commit TASK-033/045.
- **Заметки:** статус `TASK-045` → `done`, `assignee=artur`. Ход интервью описан графом LangGraph (по правилам проекта). Оговорка: полный браузерный проход цикла уточнений не выполнялся (расширение Chrome не подключено) — поведение покрыто vitest на замоканном клиенте + живым backend-e2e решения.

## TASK-033 — Восстановление сессии после обрыва (Р11)
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** [services/interview_session.py](/home/artur/projects/one-two-one/backend/app/services/interview_session.py): `session_state` возвращает первый неотвеченный core-вопрос (продолжение с того же места), `mark_current_technically_lost` создаёт `Answer(technically_lost=True)` для текущего вопроса и переводит его топик в `needs_check` (Р11: не «не подтверждено»). TTL: доступ только при действующей (не истёкшей/не отозванной) ссылке интервью, иначе 403. Эндпоинты `GET /candidate-interview/session` и `POST /candidate-interview/session/interrupt`. Фронтенд: клиент `getInterviewSession`/`interruptInterviewSession`, [InterviewPage](/home/artur/projects/one-two-one/frontend/src/pages/InterviewPage.tsx) при загрузке продолжает с текущего вопроса. Тесты — [tests/test_task_033_session.py](/home/artur/projects/one-two-one/tests/test_task_033_session.py).
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `230 passed`; `npm test` — `39 passed`. Юнит: (Шаг 1) два вопроса отвечены → возврат к третьему; (Шаг 2) прерывание → текущий вопрос `technically_lost`, топик `needs_check`, сессия завершена; (Шаг 3) истёкший `expires_at` → 403. Фронтенд-тест: восстановление продолжает с первого неотвеченного вопроса.
- **Коммиты:** общий commit TASK-033/045.
- **Заметки:** статус `TASK-033` → `done`, `assignee=artur`. Прогресс считается по ответам на core-вопросы (индекс возобновления = число отвеченных). Оговорка: браузерный сценарий «закрыть вкладку и вернуться» не прогонялся (нет расширения) — логика покрыта unit-тестами backend и vitest.

## TASK-034 — Пропуск вопроса (Р10) и подтверждение отправки с гашением ссылки
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** эндпоинт `POST /candidate-interview/questions/{id}/skip` создаёт `Answer(skipped=True)` (Р10); при оценке пропуск даёт «не подтверждено» (assess_topic уже обрабатывает skipped). В [transcription.py](/home/artur/projects/one-two-one/backend/app/services/transcription.py) добавлена защита: пропущенный ответ не транскрибируется. Финальное подтверждение и гашение ссылки переиспользуют готовый `POST /candidate-auth/submit` (TASK-026): `used_at` заполняется, кандидат → submitted, повторный вход по ссылке невозможен. Фронтенд: клиент `skipQuestion`/`submitInterview`, в [InterviewPage](/home/artur/projects/one-two-one/frontend/src/pages/InterviewPage.tsx) кнопка «Пропустить вопрос» с предупреждением о последствии, на последнем вопросе — «Завершить и отправить интервью» и экран «Интервью отправлено». Тесты — [tests/test_task_034_skip.py](/home/artur/projects/one-two-one/tests/test_task_034_skip.py), фронт — InterviewPage.test.tsx.
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `236 passed` (Шаг 1: skip → `Answer.skipped=true`; повтор → 409; чужой вопрос → 404); `npm test` — `44 passed` (предупреждение + skip-вызов; финальный экран отправки → `submitInterview` → «Интервью отправлено»); `npm run build` — успешно. Гашение ссылки (Шаг 3) покрыто существующими тестами TASK-026.
- **Коммиты:** общий commit TASK-034/038.
- **Заметки:** статус `TASK-034` → `done`, `assignee=artur`. Submit/гашение ссылки уже были реализованы Codex в TASK-026 — переиспользованы. Оговорка: браузерный проход (пропуск → отправка → недоступность ссылки) не прогонялся (нет расширения Chrome); покрыто unit-тестами backend + vitest.

## TASK-038 — UI списка кандидатов со статусами обработки и тройкой чисел
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** сервис [services/candidate_overview.py](/home/artur/projects/one-two-one/backend/app/services/candidate_overview.py) `list_vacancy_candidates` сводит статусы ответов кандидата к одному статусу обработки (not_started/recorded/transcribing/analyzing/ready/error, error приоритетнее) и добавляет тройку чисел + рекомендацию из `InterviewResult`. Эндпоинт `GET /candidates?vacancy_id=&processing_status=` (role-gated, TASK-024). Фронтенд: клиент `listCandidates`, страница [CandidateListPage](/home/artur/projects/one-two-one/frontend/src/pages/CandidateListPage.tsx) со списком, фильтром по статусу и визуальной пометкой error. AI-score нигде не выводится — только тройка чисел. Тесты — [tests/test_task_038_candidate_list.py](/home/artur/projects/one-two-one/tests/test_task_038_candidate_list.py), фронт — CandidateListPage.test.tsx.
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `236 passed` (Шаг 1: список кандидатов; Шаг 2: статусы соответствуют данным — ready/error/not_started, тройка из результата; Шаг 3: error помечен; фильтр по статусу); `npm test` — `44 passed`; `npm run build` — успешно.
- **Коммиты:** общий commit TASK-034/038.
- **Заметки:** статус `TASK-038` → `done`, `assignee=artur`. `CandidateListPage` принимает `token`/`vacancyId` пропсами; интеграция в роутинг с рекрутерской авторизацией — на фронтенде отдельно (UI входа рекрутера пока нет). Разблокирует TASK-039 (карточка результата) и TASK-046 (метрики).

## TASK-039 — UI карточки результата: матрица топиков как главный объект (M7, принцип 1)
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** сервис [services/result_card.py](/home/artur/projects/one-two-one/backend/app/services/result_card.py) `build_result_card`: матрица топиков (название, hard/soft, mandatory/desired, `system_status` отдельно от `current_status`, автор последней правки из `StatusChangeLog` или `system`), детерминированная рекомендация Р5 с кодом причины (`recommendation_with_reason` в [recommendation.py](/home/artur/projects/one-two-one/backend/app/services/recommendation.py)), тройка чисел и доли Р4, слой «заявлено в резюме» (`resume_text`). AI-score отсутствует. Эндпоинт `GET /candidates/{id}/result` (role-gated). Фронтенд: клиент `getResultCard`, страница [CandidateResultPage](/home/artur/projects/one-two-one/frontend/src/pages/CandidateResultPage.tsx) — матрица топиков первым блоком, рекомендация с расшифровкой правила Р5, отдельный блок резюме. Тесты — [tests/test_task_039_result_card.py](/home/artur/projects/one-two-one/tests/test_task_039_result_card.py), фронт — CandidateResultPage.test.tsx.
- **Как проверено:** `uv run ruff check .` — успешно; `uv run pytest` — `240 passed` (Шаг 1: матрица топиков в порядке order; Шаг 2: рекомендация + код причины Р5; Шаг 3: в `ResultCard` нет поля со «score»; автор отражает правку эксперта, `system_status` отдельно от `current_status`); `npm test` — `47 passed` (матрица первым блоком; рекомендация с расшифровкой Р5; слой резюме, отсутствие AI-score); `npm run build` — успешно.
- **Коммиты:** отдельный commit TASK-039.
- **Заметки:** статус `TASK-039` → `done`, `assignee=artur`. Рекомендация/coverage пересчитываются из `current_status` (эффективная матрица), поэтому карточка всегда актуальна после правок эксперта. `CandidateResultPage` принимает `token`/`candidateId` пропсами. Разблокирует TASK-040 (evidence+плеер) и TASK-044 (UI очереди ревью). Оговорка: браузерный проход не выполнялся (нет расширения) — покрыто backend unit + vitest + сборкой.

## TASK-040 — Evidence, переход к записи и аудит просмотра
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** кнопка статуса в матрице открывает выделенную цитату и плеер с переходом на таймкод. Приватные ссылки на видео/аудио живут 5 минут; дорожки синхронизируются. Evidence фильтруется по кандидату и топику, чужие вопросы не раскрываются. Просмотр регистрируется при playing, а не при загрузке страницы; повтор события идемпотентен, ошибка аудита останавливает плеер. Новый append-only VideoViewLog с миграцией и API чтения для трёх внутренних ролей. Карточка подключена к `/staff/candidates/{id}` с формой внутреннего входа. Подписание ссылок S3 вынесено из event loop.
- **Проверки:** Ruff — успешно; pytest — 244 passed; 4 новых API-проверки (включая три роли), 3 UI-проверки плеера проходят; TypeScript/build — успешно; mypy трёх изменённых модулей — без ошибок. Миграция применена к PostgreSQL. Все test_steps выполнены в Chrome с настоящими API/PostgreSQL/MinIO: клик статуса открыл выделенную цитату, currentTime=2, отдельное аудио воспроизводится, POST просмотра=200. В БД проверен viewer=browser-reviewer, role=technical_specialist и позиция. Скриншот: `/tmp/task040-evidence.png`.
- **Заметки:** использована синтетическая запись из проверки TASK-031. Отзываемые ссылки на результат остаются TASK-047; её статус не менялся. Существующие изменения `.gitignore` и `ralph.sh` не включаются в коммит.

## TASK-046 — Экран продуктовых метрик
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** чистая функция расчёта долей системных/текущих статусов, итоговых изменений после ревью и направлений, completion rate и технических сбоев. Отдельная SQL-выборка без ПДн, фильтр вакансии, защищённый API `/metrics/product`. Экран `/staff/metrics` и вариант по вакансии с обновлением, явными знаменателями и состоянием пустой выборки. `system_status` не меняется. Повторное ревью/несколько ошибок не дублируют топик/кандидата; возврат к системному статусу не считается итоговым изменением.
- **Проверки:** полный pytest — 250 passed; frontend — 52 passed; Ruff — успешно; mypy новых сервиса/API — без ошибок; TypeScript strict/build — успешно. Шесть новых Python-проверок охватывают пустую выборку, out_of_scope, направления, повторные реальные PATCH статуса, прерванное интервью, фильтр и роли; две UI-проверки — обновление и ошибку запроса. Все test_steps выполнены в Chrome с настоящими API и PostgreSQL: исходные метрики показаны, PATCH needs_check → confirmed обновил ревью и текущую долю при сохранении системной, добавление технически прерванного интервью снизило completion rate 100% → 50% и показало сбои 50%. Ошибок страницы нет. Скриншот `/tmp/task046-metrics.png` просмотрен.
- **Заметки:** определение каждой доли описано на экране и в README. Это срез текущих данных; метрики отсутствующих событий вроде длительности ручного ревью не выдумываются. В tasks.json изменён только status TASK-046. TASK-040 и TASK-046 выбраны как две доступные задачи на момент запроса; TASK-041/044/047 не затрагивались.

## TASK-041 — Полный транскрипт с подсветкой evidence
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** защищённый API полного транскрипта кандидата с необязательным фильтром топика. Возвращает вопросы, сегменты с таймкодами, полный текст, цитаты и состояния пропуска/технической потери. Цитаты из чужих вопросов/кандидатов/топиков исключены. В карточку добавлен отдельный блок транскрипта; подсветка точных цитат поддерживает границы сегментов и пробельные различия, не переписывая исходный текст. Резюме остаётся отдельным слоем. Предусмотрены отсутствие транскрипта, ошибка запроса и повтор загрузки.
- **Проверки:** 5 новых API-тестов и 4 frontend-теста проходят. Совместный прогон TASK-041/044: 256 pytest, 60 frontend, Ruff и TypeScript/build — успешно; mypy трёх изменённых Python-модулей — без ошибок. Все test_steps выполнены в Chrome с реальными API/PostgreSQL: открыты шесть сегментов двух ответов, цитата выделена на границе сегментов, блок резюме отдельно без подсветки. Скриншот `/tmp/task041-transcript.png` просмотрен; ошибок страницы нет.
- **Заметки:** используются синтетические данные. Подсветка не делает нечёткого поиска или предположений о несовпавших фразах. В tasks.json изменён только status TASK-041; пользовательские `.gitignore` и `ralph.sh` вне коммита.

## TASK-044 — Очередь ревью с контекстом и формой статуса
- **Дата:** 2026-09-05
- **Статус:** done
- **Что сделано:** `/staff/review` с определением роли из авторизованного API, серверной фильтрацией hard/soft и объяснением ограничений рекрутера. При открытии топика видны причина неопределённости, вопрос, транскрипт, evidence и доступный плеер. Форма трёх статусов требует содержательного комментария; API тоже отклоняет пробелы. Ошибка сохранения оставляет введённые данные. Успешная правка заново загружает очередь и карточку с обновлённым статусом/автором. Используются RBAC и append-only аудит TASK-042/043, system_status не меняется.
- **Проверки:** совместный полный прогон — 256 pytest и 60 frontend-тестов, Ruff, mypy и TypeScript/build успешно. Новый HTTP-сценарий проверяет hard/soft, контекст, пустые комментарии, запрет чужой роли/рекрутера, исходный системный статус и обновлённую карточку. Четыре UI-теста проверяют контекст/форму/перезагрузку, ошибку сохранения, рекрутера и повтор загрузки. Все test_steps выполнены в Chrome с реальной БД/API: техспециалист увидел hard-очередь, открыл вопрос/ответ/цитату/причину, сохранил confirmed с комментарием, топик ушёл из очереди, карточка показала confirmed и автора. Дополнительно проверены soft-очередь менеджера и ограничения рекрутера. Скриншот `/tmp/task044-reviewed.png` просмотрен; ошибок страницы нет.
- **Заметки:** только синтетические данные. В tasks.json изменён status TASK-044; TASK-047 остаётся pending. Пользовательские `.gitignore` и `ralph.sh` не включены.
