# Progress Log — ИИ-интервьюер

Журнал прогресса агентов. Каждый агент после завершения задачи добавляет запись сюда.

Формат записи:

```
## TASK-XXX — <краткое название>
- **Дата:** YYYY-MM-DD
- **Статус:** done
- **Что сделано:** краткое описание изменений
- **Как проверено:** результат прохождения test_steps
- **Коммиты:** <хэши>
- **Заметки:** отклонения, TODO, обнаруженные проблемы
```

---

<!-- Записи агентов добавляются ниже -->

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** добавлены `backend/app`, `frontend/src`, `infra`, корневой `docker-compose.yml` с `postgres` и `redis`, `pyproject.toml` для `ruff`/`pytest`, базовый тест `tests/test_task_001_scaffold.py` на структуру скаффолда и наличие ключевых конфигов; в тесте добавлен модульный docstring, чтобы проходил `ruff`.
- **Как проверено:** `python3 -m py_compile backend/app/__init__.py tests/test_task_001_scaffold.py` проходит; `ruff check /home/artur/projects/one-two-one` из доступного локального venv проходит; `pytest /home/artur/projects/one-two-one/tests/test_task_001_scaffold.py` проходит (`3 passed`); прямой запуск `uv run ruff check .` и `uv run pytest` остаётся заблокированным, потому что в проектном окружении нет установленных пакетов, а `uv sync` не может скачать их из-за недоступности `pypi.org` в этой среде; `docker compose` test_steps не выполнены, потому что `docker` отсутствует в окружении.
- **Коммиты:** будет создан отдельный commit по `TASK-001` после обновления журнала
- **Заметки:** статус `tasks.json` оставлен `in_progress`; для закрытия задачи следующей итерации нужно запустить именно `uv run ruff check .`, `uv run pytest` в среде с доступом к пакетам Python и выполнить все три шага `docker compose` из `TASK-001` на хосте, где установлен Docker.

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** поднят локальный toolchain внутри проектного `.venv`: установлены `uv`, `ruff` и `pytest`, после чего дополнительно перепроверен уже существующий scaffold без изменений в коде задачи.
- **Как проверено:** `.venv/bin/uv run ruff check .` проходит (`All checks passed!`); `.venv/bin/uv run pytest` проходит (`3 passed`); обязательные `test_steps` из `tasks.json` всё ещё не закрыты, потому что в текущем окружении отсутствует `docker`, а значит нельзя выполнить `docker compose up -d`, проверить `healthy` у контейнеров и сделать `psql`/`redis-cli` smoke test.
- **Коммиты:** будет создан отдельный commit по обновлению журнала прогресса
- **Заметки:** статус `TASK-001` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) оставлен `in_progress`; следующей итерации нужен хост с установленным Docker для прохождения трёх acceptance `test_steps` без изменений в репозитории.

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** уточнён конфиг `ruff` в `pyproject.toml`: из lint scope исключён `.codex-home`, чтобы dev-проверка охватывала только код и конфиги репозитория в рамках `TASK-001`.
- **Как проверено:** `uv run ruff check .` проходит (`All checks passed!`); `uv run pytest` проходит (`3 passed`); `docker --version` доступен; `docker compose up -d` стартовал, но упёрся в скачивание `redis:7-alpine` из Docker Hub с ошибкой `network is unreachable`; `docker compose down -v` выполнен для cleanup.
- **Коммиты:** будет создан отдельный commit по обновлению конфигурации и журнала прогресса
- **Заметки:** `TASK-001` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) оставлен `in_progress`; для завершения задачи следующей итерации нужно повторить `docker compose up -d`, дождаться `healthy` у `postgres` и `redis`, затем выполнить smoke check через `psql` и `redis-cli` внутри контейнеров в среде с доступом Docker к registry.

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** для `redis` в [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) заменён образ `redis:7-alpine` на официальный mirror `public.ecr.aws/docker/library/redis:7-alpine`, чтобы обойти сбой pull из Docker Hub по IPv6 и сохранить тот же Redis 7 Alpine в dev-окружении.
- **Как проверено:** `UV_CACHE_DIR=/tmp/uv-cache ./.venv/bin/uv run ruff check .` проходит (`All checks passed!`); `UV_CACHE_DIR=/tmp/uv-cache ./.venv/bin/uv run pytest` проходит (`3 passed`); хостовый `docker pull postgres:16-alpine` ранее проходит, а `docker pull redis:7-alpine` падал на `registry-1.docker.io ... network is unreachable`; проверка обновлённого compose внутри этой среды не выполнена, потому что shell-инструменты текущей сессии не имеют доступа к хостовому `docker`.
- **Коммиты:** `b7bdde6` (`feat: add S3 storage and Celery integration scaffolding`)
- **Заметки:** `TASK-001` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) оставлен `in_progress`; после этой правки нужно повторить `docker compose up -d`, дождаться `healthy` у `postgres` и `redis`, затем выполнить smoke check через `psql` и `redis-cli` на хосте.

## TASK-001 — Скаффолд монорепо и dev-окружение
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** пройдены все `test_steps` на хосте с рабочим Docker. Docker Hub снова доступен (`docker pull redis:7-alpine` проходит), поэтому в [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) убран временный mirror `public.ecr.aws/docker/library/redis:7-alpine` и возвращён канонический `redis:7-alpine` — симметрично `postgres:16-alpine`. Зафиксирован `uv.lock` для воспроизводимости dev-зависимостей (`ruff`, `pytest`).
- **Как проверено:** `docker compose up -d` — оба контейнера стартуют без ошибок; `docker inspect` показывает `postgres=healthy redis=healthy`; `docker exec interviewer-postgres psql -U interviewer -d interviewer -c "SELECT 1"` возвращает строку, `docker exec interviewer-redis redis-cli PING` -> `PONG`, `SET/GET/DEL` работают; порты `5432` и `6379` доступны с хоста; `docker compose down` удаляет контейнеры и сеть, `docker ps -a --filter name=interviewer-` пуст. `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `3 passed`. Acceptance criteria закрыты: compose поднимается, `.env.example` содержит все ключи, структура `backend/app`, `frontend/src`, `infra`, `docs` на месте.
- **Коммиты:** `feat: complete TASK-001 dev environment scaffold` (последний коммит ветки `artur`)
- **Заметки:** статус `TASK-001` переведён в `done`. Следующая задача по критическому пути — `TASK-002` (скелет FastAPI: `app/main.py`, `config.py` на pydantic-settings, CORS, `/health`); её зависимость `TASK-001` теперь выполнена. `backend/app` пока содержит только пустой `__init__.py` — приложение появляется в `TASK-002`. Если pull образов снова упрётся в Docker Hub по IPv6, рабочий обходной путь — mirror `public.ecr.aws/docker/library/<image>`.

## TASK-002 — Скелет FastAPI: config, CORS, /health
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** добавлены [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) (`Settings` на pydantic-settings, `env_file` привязан к корню репо, `extra="ignore"`, кеш через `get_settings()`) и [backend/app/main.py](/home/artur/projects/one-two-one/backend/app/main.py) (фабрика `create_app`, `CORSMiddleware` из `cors_origins`, async-эндпоинт `GET /health` с pydantic-моделью `HealthResponse`). В `pyproject.toml` добавлены рантайм-зависимости `fastapi`, `uvicorn[standard]`, `pydantic-settings`, dev-зависимость `httpx`, а также `pytest.pythonpath = ["backend"]` и `ruff.src = ["backend"]`, чтобы `app.*` резолвился как first-party. В `.env.example` добавлен ключ `CORS_ORIGINS` (JSON-список). Тесты — [tests/test_task_002_api.py](/home/artur/projects/one-two-one/tests/test_task_002_api.py). README дополнен секцией «Запуск backend».
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `7 passed`. Все три `test_steps` выполнены вживую: (1) `uv run uvicorn app.main:app --app-dir backend --port 8123` стартует без ошибок (`Application startup complete`); (2) `curl /health` -> `HTTP 200` и `{"status":"ok","app_env":"development","version":"0.1.0"}`; (3) `curl /docs` -> `HTTP 200` с разметкой `swagger-ui`, `/openapi.json` содержит путь `/health`. Дополнительно: preflight `OPTIONS /health` с `Origin: http://localhost:5173` возвращает `access-control-allow-origin`, а запуск с `APP_ENV=staging` отдаёт `"app_env":"staging"` — конфигурация действительно читается из окружения.
- **Коммиты:** `feat: add FastAPI skeleton with config and health endpoint`
- **Заметки:** статус `TASK-002` переведён в `done`. Приложение импортируется как `app.main:app` при `--app-dir backend` (пакет `backend/app`) — этот же путь понадобится для Celery и Alembic. `cors_origins` задаётся JSON-списком в `.env`, ручного парсинга строки нет намеренно. Следующая задача критического пути — `TASK-003` (PostgreSQL + Alembic, `get_db`): БД-настройки стоит добавить в тот же `Settings` (`DATABASE_URL` уже есть в `.env.example`), сессию — async (`asyncpg` в URL). `TestClient` печатает `StarletteDeprecationWarning` про `httpx2` — не блокирует, при желании поменять dev-зависимость позже.

## TASK-003 — PostgreSQL + Alembic + зависимость get_db
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** добавлен слой доступа к БД [backend/app/db/](/home/artur/projects/one-two-one/backend/app/db): `base.py` (`Base` на `DeclarativeBase` — общая метадата для моделей и Alembic) и `session.py` (async-движок `create_async_engine` с `pool_pre_ping`, фабрика `async_sessionmaker`, зависимость `get_db`, `dispose_engine` для закрытия пула). Движок и фабрика кешируются через `lru_cache`, поэтому в тестах их можно перенацелить сбросом кеша. В `Settings` добавлены `database_url` и `database_echo`, в `.env.example` — `DATABASE_ECHO`. В [backend/app/main.py](/home/artur/projects/one-two-one/backend/app/main.py) появились `lifespan` (освобождает пул на остановке сервиса) и пробный эндпоинт `GET /health/db`, который выполняет `SELECT 1` через сессию из `get_db`. Alembic настроен на корневой [alembic.ini](/home/artur/projects/one-two-one/alembic.ini) (`script_location=%(here)s/backend/alembic`, `prepend_sys_path=%(here)s/backend`, `sqlalchemy.url` намеренно убран), окружение — [backend/alembic/env.py](/home/artur/projects/one-two-one/backend/alembic/env.py) на async-движке с URL из `Settings`; `script.py.mako` переписан под стиль проекта (`str | Sequence[str] | None`, русские докстринги), чтобы автогенерируемые ревизии проходили `ruff`. Первая ревизия `11d73faaed8f_initial_baseline` — базовая точка отсчёта цепочки (таблицы доменной модели идут в `TASK-004`). Зависимости: `sqlalchemy[asyncio]`, `alembic`, `asyncpg`, dev — `aiosqlite`. Тесты — [tests/test_task_003_db.py](/home/artur/projects/one-two-one/tests/test_task_003_db.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `12 passed`. Все три `test_steps` выполнены вживую на поднятом `docker compose` (postgres/redis `healthy`): (1) `uv run alembic upgrade head` -> `Running upgrade -> 11d73faaed8f`, `alembic current` = `11d73faaed8f (head)`, в psql видна таблица `alembic_version` с этой версией; (2) `uvicorn app.main:app --app-dir backend --port 8124` стартует, `curl /health/db` -> `HTTP 200` и `{"status":"ok","database":"ok"}`, путь `/health/db` есть в `/openapi.json`; (3) `uv run alembic downgrade base` -> `Running downgrade 11d73faaed8f -> `, `alembic current` пуст, `SELECT count(*) FROM alembic_version` = 0, повторный `upgrade head` снова даёт `head`. Дополнительно: `docker compose stop postgres` переводит `/health/db` в `HTTP 500`, после `start` эндпоинт снова отдаёт 200 — запрос действительно ходит в БД, а `pool_pre_ping` переживает рестарт; `alembic upgrade head --sql` (offline-режим) генерирует корректный SQL.
- **Коммиты:** `feat: add PostgreSQL session layer and Alembic baseline`
- **Заметки:** статус `TASK-003` переведён в `done`. Для `TASK-004` важно: `env.py` берёт метадату из `app.db.Base`, поэтому новые ORM-модели **нужно импортировать**, чтобы `--autogenerate` их увидел — заведи `backend/app/models/__init__.py` с реэкспортом моделей и добавь его импорт в `env.py`, иначе автогенерация выдаст пустую ревизию. Все `id` в PRD — `uuid`; расширение для этого не нужно, в Postgres 16 `gen_random_uuid()` встроен. Тесты гоняют `get_db` поверх `sqlite+aiosqlite:///:memory:`, чтобы `uv run pytest` не требовал Postgres, — если в задаче понадобятся Postgres-специфичные типы (`jsonb`, enum), тест придётся переводить на реальную БД. В рабочем дереве остаются несвязанные с задачей правки `.gitignore` и `ralph.sh` (тулинг Codex) — в коммит `TASK-003` они намеренно не попали.

## TASK-004 — ORM-модели Vacancy и Topic + миграция
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** заведён пакет [backend/app/models/](/home/artur/projects/one-two-one/backend/app/models) с моделями раздела 5 PRD: [vacancy.py](/home/artur/projects/one-two-one/backend/app/models/vacancy.py) (`Vacancy` + enum `VacancyGrade` junior/middle/middle+/senior и `VacancyStatus` draft/active/archived; поля `title`, `tasks`, `stop_factors` как `text[]`, `specialist_profile`, `version` с дефолтом 1) и [topic.py](/home/artur/projects/one-two-one/backend/app/models/topic.py) (`Topic` + enum `SkillType` hard/soft и `TopicImportance` mandatory/desired; `requirement_description`, `depth_expectations`, `verifiable_by_interview` с дефолтом `true`, `order`, FK `vacancy_id -> vacancy.id` с `ON DELETE CASCADE` и индексом). Связь `Vacancy.topics` — `lazy="selectin"` (async-безопасно), `order_by="Topic.order"`, `cascade="all, delete-orphan"`. В [app/db/base.py](/home/artur/projects/one-two-one/backend/app/db/base.py) добавлены `TimestampMixin` (`created_at`/`updated_at` с `server_default=now()`) и хелпер `enum_column`, который через `values_callable` заставляет SQLAlchemy писать в БД **значения** членов enum, а не их имена — иначе в колонке оказалось бы `MIDDLE_PLUS` вместо `middle+`. В [alembic/env.py](/home/artur/projects/one-two-one/backend/alembic/env.py) добавлен импорт `app.models`, чтобы `--autogenerate` видел модели. Ревизия `f7db7e0c8e3e_vacancy_and_topic_tables` (down_revision — baseline `11d73faaed8f`); в `downgrade()` типы enum дропаются явным циклом, потому что `op.drop_table` их не удаляет и повторный `upgrade` падал бы на `type already exists`. Тесты — [tests/test_task_004_models.py](/home/artur/projects/one-two-one/tests/test_task_004_models.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `22 passed`; `uv run alembic check` — `No new upgrade operations detected` (модели и миграция не разъехались). Все три `test_steps` выполнены вживую на поднятом compose: (1) `uv run alembic upgrade head` -> `Running upgrade 11d73faaed8f -> f7db7e0c8e3e`, `alembic current` = `f7db7e0c8e3e (head)`; (2) `\d vacancy` и `\d topic` показывают все поля PRD, типы `vacancy_grade`/`vacancy_status`/`topic_skill_type`/`topic_importance`, дефолты `'{}'::text[]`, `1`, `'draft'`, `true`, `0`, индекс `ix_topic_vacancy_id` и FK `topic_vacancy_id_fkey ... ON DELETE CASCADE`; (3) через psql вставлены вакансия с грейдом `middle+` и связанный топик `Kafka в production` — `SELECT` с `JOIN` вернул обе записи. Дополнительно: вставка топика с несуществующим `vacancy_id` отбита FK, `grade='lead'` отбит enum-типом, `DELETE FROM vacancy` каскадом снёс топик; ORM-прогон на реальном Postgres (asyncpg) вернул `VacancyGrade.MIDDLE_PLUS`, `status=draft`, `version=1`, `stop_factors` как список строк и оба топика в порядке `order`; `alembic downgrade -1` снёс обе таблицы и все 4 enum-типа (`pg_type` -> 0), повторный `upgrade head` снова поднял схему.
- **Коммиты:** `feat: add Vacancy and Topic ORM models with migration`
- **Заметки:** статус `TASK-004` переведён в `done`. Тест `test_single_initial_revision_exists` из `TASK-003` жёстко требовал ровно одну ревизию в `versions/` — переименован в `test_single_baseline_revision_exists` и теперь проверяет, что базовая ревизия (`down_revision = None`) в цепочке одна; иначе каждая новая миграция ломала бы его. Для `TASK-005`/`TASK-006`: модели складывать в `app/models/<сущность>.py` и обязательно реэкспортировать в `app/models/__init__.py`, иначе autogenerate их не увидит; enum-колонки объявлять только через `enum_column(...)`; в `downgrade()` новых ревизий не забывать явный дроп enum-типов. Тесты гоняются на `sqlite+aiosqlite`, поэтому Postgres-специфичные типы объявляй с `.with_variant(...)` — так сделан `stop_factors` (`ARRAY(Text).with_variant(JSON, "sqlite")`); для `jsonb` в `TASK-006` понадобится `JSONB().with_variant(JSON, "sqlite")`. В рабочем дереве остаются несвязанные с задачей правки `.gitignore` и `ralph.sh` (тулинг Codex) — в коммит они намеренно не попали.

## TASK-005 — ORM-модели Candidate, InterviewLink, Question + миграция
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** добавлены три модели раздела 5 PRD. [candidate.py](/home/artur/projects/one-two-one/backend/app/models/candidate.py) — `Candidate` (FK `vacancy_id` с `ON DELETE CASCADE`, `resume_text`, `resume_file_url`, `consent_given_at`, enum `CandidateStatus` invited/in_progress/submitted/processed/reviewed с дефолтом `invited`). [interview_link.py](/home/artur/projects/one-two-one/backend/app/models/interview_link.py) — `InterviewLink` (FK `candidate_id` CASCADE, `token` с уникальным индексом, `expires_at`, `used_at`, `revoked` с дефолтом `false`); поля Р20 на месте, логика выпуска/гашения ссылки — в TASK-026. [question.py](/home/artur/projects/one-two-one/backend/app/models/question.py) — `Question` (FK `topic_id` CASCADE — ровно один топик по Р16, enum `QuestionType` core/personal/follow_up и `QuestionPattern` technical/experience/reasoning, `text`, `source_reason`, `reviewed_by_expert`, self-FK `parent_question_id -> question.id` CASCADE, nullable, с индексом) и self-referential связка `parent`/`follow_ups` через `remote_side="Question.id"`. Все три реэкспортированы в `app/models/__init__.py`. Ревизия `a7feac956a32_candidate_link_and_question_tables` (down_revision — `f7db7e0c8e3e`), в `downgrade()` дропаются enum-типы `candidate_status`, `question_type`, `question_pattern`. Тесты — [tests/test_task_005_models.py](/home/artur/projects/one-two-one/tests/test_task_005_models.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run ruff format --check .` — `29 files already formatted`; `uv run pytest` — `34 passed`; `uv run alembic check` — `No new upgrade operations detected`. Все три `test_steps` выполнены вживую на поднятом compose: (1) `uv run alembic upgrade head` -> `Running upgrade f7db7e0c8e3e -> a7feac956a32`, `alembic current` = `a7feac956a32 (head)`, `\d candidate`/`\d interview_link`/`\d question` показывают все поля PRD, дефолты `'invited'::candidate_status` и `false`, уникальный индекс `ix_interview_link_token` и FK `question_parent_question_id_fkey ... REFERENCES question(id) ON DELETE CASCADE` (self-FK виден и в `Referenced by`); (2) через psql вставлены вакансия, топик, `candidate` (`status=in_progress`), `interview_link` и core-вопрос — `SELECT` с `JOIN` вернул все связи; (3) вставлен `follow_up`-вопрос с `parent_question_id` core-вопроса — self-join `question -> question` вернул пару child/parent. Дополнительно: несуществующий `parent_question_id` отбит self-FK, `status='hired'` отбит enum-типом, дубль `token` отбит уникальным индексом, `DELETE` core-вопроса каскадом снёс уточнение, `DELETE` вакансии каскадом снёс кандидата и ссылку; ORM-прогон на реальном Postgres (asyncpg) вернул `CandidateStatus.INVITED`, `revoked=False`, `used_at=None`, `reviewed_by_expert=False` и `follow_ups` с корректным `parent_question_id`; `alembic downgrade -1` снёс 3 таблицы и 3 enum-типа (`pg_type` -> 0), повторный `upgrade head` снова поднял схему.
- **Коммиты:** `feat: add Candidate, InterviewLink and Question ORM models with migration`
- **Заметки:** статус `TASK-005` переведён в `done`. Следующая задача критического пути — `TASK-006` (`Answer`, `TopicAssessment`, `StatusChangeLog`, `InterviewResult`): её единственная зависимость `TASK-005` теперь выполнена. Для неё важно: `jsonb`-поля (`transcript_segments`, `signals`, `evidence`) объявлять как `JSONB().with_variant(JSON, "sqlite")`, иначе тесты на `sqlite+aiosqlite` не поднимут схему; `TopicAssessment.system_status` и `current_status` — две отдельные колонки одного enum-типа (инвариант 4: `system_status` не перезаписывается никогда), `StatusChangeLog` — без `TimestampMixin` c `onupdate`, только `created_at`, чтобы append-only читался из схемы. По полям PRD у `Candidate` нет имени/контакта — намеренно не добавлял, отправка ссылки появится в `TASK-026`. В `question.py` `sqlalchemy.text` импортирован как `sql_text`, потому что колонка называется `text` и перекрыла бы имя в теле класса. В рабочем дереве остаются несвязанные с задачей правки `.gitignore` и `ralph.sh` (тулинг Codex) — в коммит они намеренно не попали.

## TASK-006 — ORM-модели Answer, TopicAssessment, StatusChangeLog, InterviewResult + миграция
- **Дата:** 2026-09-04
- **Статус:** done
- **Что сделано:** добавлены [answer.py](/home/artur/projects/one-two-one/backend/app/models/answer.py) (`Answer` + enum `AnswerProcessingStatus` и `transcript_segments` на `JSONB().with_variant(JSON, "sqlite")`), [topic_assessment.py](/home/artur/projects/one-two-one/backend/app/models/topic_assessment.py) (`TopicAssessment` с отдельными `system_status`/`current_status`, enum-ами `AssessmentStatus`/`AssessmentConfidence`, `signals` и `evidence` в JSONB, `StatusChangeLog` с `author_role`, `old_status`, `new_status` и только `created_at` для append-only формы) и [interview_result.py](/home/artur/projects/one-two-one/backend/app/models/interview_result.py) (`InterviewResult` с детерминированной рекомендацией, coverage и версиями воспроизводимости). В существующие модели добавлены связи: `Question.answers`, `Candidate.topic_assessments`, `Candidate.interview_result`, `Topic.assessments`. Реэкспорт обновлён в [backend/app/models/__init__.py](/home/artur/projects/one-two-one/backend/app/models/__init__.py). Добавлена ревизия [4ae9c5a1f2d0_answer_assessment_and_result_tables.py](/home/artur/projects/one-two-one/backend/alembic/versions/4ae9c5a1f2d0_answer_assessment_and_result_tables.py) и тесты [tests/test_task_006_models.py](/home/artur/projects/one-two-one/tests/test_task_006_models.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `46 passed`. Все три `test_steps` выполнены на живом Postgres в Docker: (1) `DATABASE_URL=postgresql+asyncpg://interviewer:interviewer@localhost:5432/interviewer uv run alembic upgrade head` применил ревизию `4ae9c5a1f2d0`, `alembic current` и `SELECT version_num FROM alembic_version` показывают `4ae9c5a1f2d0`; (2) через `docker exec ... psql` вставлен `topic_assessment` с `signals` и `evidence` в `jsonb`, `SELECT` вернул JSON с цитатой и `question_id`; (3) `\d topic_assessment` показывает обе колонки `system_status` и `current_status`, а также `jsonb`-поля `signals`/`evidence` и уникальность `(candidate_id, topic_id)`.
- **Коммиты:** `b7bdde6` (`feat: add S3 storage and Celery integration scaffolding`)
- **Заметки:** `StatusChangeLog` оставлен без `updated_at`, чтобы append-only было видно уже из схемы и ORM; для sqlite-тестов `created_at` сделан через `func.now()`, иначе вставка падала на `unknown function: now()`. Следующий логический шаг после `TASK-006` — начать слой интеграции/сервисов, который будет реально соблюдать инвариант «`system_status` не перезаписывается», потому что текущая задача завершает именно схему, миграцию и покрывающие тесты.

## TASK-007 — LangChain ChatOpenAI wrapper с версионированием
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** добавлен пакет [backend/app/integrations/](/home/artur/projects/one-two-one/backend/app/integrations) с [llm.py](/home/artur/projects/one-two-one/backend/app/integrations/llm.py): `LangChainLLMClient` поверх `langchain_openai.ChatOpenAI`, выбор quality/fast модели через `LLM_MODEL` и `LLM_FAST_MODEL`, чтение `OPENAI_API_KEY`/`OPENAI_BASE_URL`/timeout из `Settings`, `LLMInvocationResult` с `model_version` и `prompt_version`, а также типизированное исключение `LLMClientError`. Structured output реализован только через native `with_structured_output(..., method="json_schema", strict=True)` без ручного парсинга JSON. В [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) добавлены LLM-настройки, в `pyproject.toml` и `uv.lock` — зависимость `langchain-openai`. Юнит-тесты — [tests/test_task_007_llm_client.py](/home/artur/projects/one-two-one/tests/test_task_007_llm_client.py).
- **Как проверено:** `uv run ruff check .` — `All checks passed!`; `uv run pytest` — `50 passed`. Ручной smoke для шага 3 и 4 проходит: локальный OpenAI-совместимый stub на `http://127.0.0.1:8765/v1` отрабатывает без изменений кода (`LOCAL_SWITCH_OK local-stub-model-v1 task-007-local-switch-v1 'local stub ok'`), а вызов на несуществующий endpoint `http://127.0.0.1:8999/v1` возвращает `LLMClientError` (`NETWORK_ERROR_OK LLM provider request failed local-stub task-007-network-error-v1`). Шаги 1-2 против текущего OpenRouter-конфига в этой среде не закрыты: реальный вызов на `https://openrouter.ai/api/v1` завершился `langchain_openai.chat_models.base.OpenAIConnectionError: Connection error`, который корректно завернулся в `LLMClientError`.
- **Коммиты:** pending
- **Заметки:** `TASK-007` в [tasks.json](/home/artur/projects/one-two-one/tasks.json) оставлен `in_progress`, потому что не пройдены все `test_steps`: нужно повторить живой smoke на OpenRouter из сети, где endpoint отвечает стабильно, и отдельно проверить structured output на реальном провайдере (шаги 1-2). Код под это уже готов: достаточно запустить короткий async-скрипт с `LangChainLLMClient().generate_text(...)` и `generate_structured(..., schema=...)` на текущем `.env`. Если OpenRouter продолжит рвать TCP/HTTP до ответа, стоит проверить прокси/egress этой среды и, при необходимости, добавить provider-specific headers только если без них реальный curl подтвердит проблему именно авторизации/маршрутизации, а не сетевого уровня.

## TASK-008 — S3/MinIO wrapper с presigned URL и multipart
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** добавлен [backend/app/integrations/storage.py](/home/artur/projects/one-two-one/backend/app/integrations/storage.py) с `S3StorageClient`: загрузка объекта, скачивание байтов, presigned GET URL, `ensure_bucket()` для dev-MinIO, а также multipart-flow (`create_multipart_upload`, `upload_part`, `complete_multipart_upload`, `abort_multipart_upload`). В [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) добавлены S3-настройки (`S3_ENDPOINT_URL`, `S3_REGION`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, `S3_BUCKET`, TTL presigned URL, path-style addressing для MinIO), в [backend/app/integrations/__init__.py](/home/artur/projects/one-two-one/backend/app/integrations/__init__.py) — экспорты клиента, в [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) — `minio` и `minio-init`, а в [backend/Dockerfile](/home/artur/projects/one-two-one/backend/Dockerfile) подготовлена база для контейнерных сервисов, использующих `uv`.
- **Как проверено:** `uv run ruff check backend/app/config.py backend/app/integrations/__init__.py backend/app/integrations/storage.py tests/test_task_008_storage.py` — `All checks passed!`; `uv run pytest tests/test_task_008_storage.py -q` — `3 passed`. Тесты покрывают put/get roundtrip, presigned URL generation, path-style конфиг для MinIO и multipart completion в in-memory fake S3 client.
- **Коммиты:** pending
- **Заметки:** `TASK-008` не переведена в `done`, потому что не выполнены живые `test_steps` против реального MinIO/S3: нужно поднять `docker compose --profile storage up -d minio minio-init`, затем сделать put/get и открыть presigned URL в браузере. Код и compose-конфигурация для этого готовы.

## TASK-009 — Celery + Redis: app, worker, debug_task
- **Дата:** 2026-09-04
- **Статус:** in_progress
- **Что сделано:** добавлен [backend/app/celery_app.py](/home/artur/projects/one-two-one/backend/app/celery_app.py) с `create_celery_app()`, кешированным `celery_app` и зарегистрированной `debug_task`, которая логирует выполнение и возвращает `task_id/payload/status`. В [backend/app/config.py](/home/artur/projects/one-two-one/backend/app/config.py) добавлены `REDIS_URL`, `CELERY_BROKER_URL`, `CELERY_RESULT_BACKEND`; в [docker-compose.yml](/home/artur/projects/one-two-one/docker-compose.yml) — сервис `celery-worker` с профилем `worker`; в [pyproject.toml](/home/artur/projects/one-two-one/pyproject.toml) и [uv.lock](/home/artur/projects/one-two-one/uv.lock) — зависимости `celery[redis]` и `boto3`.
- **Как проверено:** `uv run ruff check backend/app/config.py backend/app/celery_app.py tests/test_task_009_celery.py` — `All checks passed!`; `uv run pytest tests/test_task_009_celery.py -q` — `2 passed`. Тесты подтверждают чтение broker/result backend из `Settings`, регистрацию `app.debug_task` и выполнение `.delay(...).get()` в eager smoke-режиме.
- **Коммиты:** pending
- **Заметки:** `TASK-009` не переведена в `done`, потому что не прогнаны живые compose-шаги: нужно поднять `docker compose --profile worker up -d celery-worker`, вызвать `debug_task.delay()` в среде с доступным Redis и проверить логи воркера. Конфигурация для этого уже добавлена.
