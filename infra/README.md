# Infra

Docker Compose for local Postgres and Redis lives at the repo root
(`docker-compose.yml`) so `docker compose up` works from the project root.

Additional infra (CI, k8s, Terraform) will land here later.
